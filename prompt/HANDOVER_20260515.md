# Handover 2026-05-15

## DLNA SQLite Index Hardening

- Hardened the SQLite-backed DLNA media index in `utils/media_index.py` for future schema and operational edge cases.
- Added explicit schema handling:
  - `SCHEMA_VERSION` is mirrored into both `meta.schema_version` and SQLite `PRAGMA user_version`;
  - startup validates required tables/columns;
  - future/newer schema versions are treated as incompatible and rebuilt instead of being silently overwritten;
  - `_migrate_schema()` now exists as the forward migration hook for future field additions.
- Added database recovery:
  - corrupt or incompatible `library_index.sqlite` files are moved aside with a `.corrupt_YYYYMMDD_HHMMSS` suffix;
  - related WAL/SHM files are also handled;
  - if a database operation fails during Browse, the index rebuilds and retries once.
- Added Windows-specific safety after testing:
  - `_open_connection()` now closes a partially opened sqlite handle if PRAGMA setup fails, avoiding locked corrupt files that cannot be renamed.
- Added SQLite operational guardrails:
  - connection timeout and `busy_timeout` are set;
  - stale-entry deletion now uses a temporary table instead of a long `NOT IN (?, ?, ...)` list, avoiding SQLite parameter-count limits in large directories.
- Added scan edge-case protections:
  - directories outside configured media roots are rejected cleanly;
  - large directory scans are logged for diagnostics;
  - newly modified video files are marked `pending` for a short grace window before probing, reducing ffprobe work while files are still being copied;
  - pending probes retry after 2 seconds, real probe failures retry after 60 seconds;
  - per-directory probe failure logging is capped to avoid log floods from many bad files.
- Validation:
  - `python -m compileall config.py utils\media_index.py dlna\content_directory.py` passed.
  - Normal cache smoke test passed: repeated Browse hits the same snapshot; adding a file changes the signature and returns the new file.
  - Corrupt SQLite file test passed: bad DB is moved aside and rebuilt.
  - Future schema version test passed: `user_version=999` DB is treated as incompatible and rebuilt.
  - Large directory test with 1100 child directories passed, covering the previous SQLite parameter-limit risk.

## DLNA Client Probe Tool

- Added `tools/dlna_client_probe.py` to simulate a real DLNA client path:
  - sends SOAP `BrowseDirectChildren` to `/control/cds`;
  - recursively locates a video by title/path fragment;
  - defaults to selecting the alpha live item (`*_FISHEYE180_alpha_live` / `mode=alpha`);
  - supports `--prefer green` to select the normal `*-passthrough-live` item;
  - if the live item is a chapter container, browses into it and selects the first chapter or `--chapter-index`.
- Playback simulation:
  - GETs the DIDL `<res>` URL returned by the server, so it exercises real generated URLs rather than hard-coded endpoint guesses;
  - supports profiles: `skybox`, `moonvr`, `nplayer`, `quest`, `lavf`, `default`;
  - profile headers drive existing server branches such as `libmpv`, `vlc`, `nPlayer`, `quest_dalvik`, and `lavf`;
  - supports duplicate startup probes and MoonVR-style Lavf side Range probes.
- Metrics:
  - prints JSON with selected DIDL item, HTTP status, response headers, first-byte time, bytes read, elapsed time, and average bitrate;
  - optional `--out` writes the JSON to disk for future auto-tuning runs.
- Example commands:
  - `uv run python tools\dlna_client_probe.py "movie.mp4" --profile skybox -d 20`
  - `uv run python tools\dlna_client_probe.py "movie.mp4" --profile moonvr --with-lavf-side-probes -d 20`
  - `uv run python tools\dlna_client_probe.py "movie.mp4" --profile nplayer --duplicate-startup 2 -d 20`
  - `uv run python tools\dlna_client_probe.py "movie.mp4" --prefer green --profile skybox -d 20`
- Validation:
  - `python -m compileall tools\dlna_client_probe.py` passed.
  - A mocked Browse tree confirmed default selection chooses the alpha live chapter URL and `--prefer green` chooses the normal passthrough live URL.
  - `python tools\dlna_client_probe.py --help` prints the expected CLI.

## 8K Automation Plan Documentation

- Updated `summary/summary_20260515_IMPL_PLAN_8K_40FPS_EN.md` and `summary/summary_20260515_IMPL_PLAN_8K_40FPS_ZH.md` so the 8K/40fps optimization plan no longer depends on manual real DLNA client playback as a phase gate.
- Added Phase 0 automation prerequisites:
  - reuse `tools/pynv_fullchain_probe.py`, `tools/pynv_decode_probe.py`, `tools/bench.py`, and `tools/dlna_client_probe.py`;
  - add a future `tools/auto_tune_8k.py` orchestrator to start/stop the server, run automated DLNA Browse + live HTTP pulls, parse `debug_output/server.log`, and write `baseline/` reports;
  - extend `tools/pynv_fullchain_probe.py` with `--pipeline=serial|staged` and JSON output before using it as a Phase 4 gate.
- Replaced Phase 1 and Phase 4 manual playback requirements with automated `auto_tune_8k.py` gates. Physical Quest/DLNA device playback is now optional compatibility soak after automated gates pass.
