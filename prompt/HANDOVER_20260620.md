# HANDOVER 2026-06-20

## NVDS Feasibility Review

- User placed NVDS source under `reference/NVDS` and checkpoint under
  `models/NVDS/NVDS_Stabilizer.pth`.
- Confirmed checkpoint exists and is a PyTorch zip checkpoint:
  `models/NVDS/NVDS_Stabilizer.pth`, 354,794,886 bytes.
- Current project runtime remains ONNX/ONNX Runtime based:
  - `.venv` Python is 3.12.12.
  - `onnx` and `onnxruntime` are installed.
  - `torch`, `timm`, and `mmcv` are not installed.
- NVDS source is official PyTorch code. Direct runtime integration would violate
  the ONNX-only runtime constraint and is not compatible with the current
  Python 3.12 environment because NVDS pins old `torch==1.9.0+cu111`.
- Practical path is export-only:
  1. Build a separate temporary export environment, likely Python 3.8/3.9.
  2. Install minimal PyTorch export dependencies (`torch`, `timm`, `einops`,
     `opencv-python`, `attr`, plus any import-only small packages).
  3. Load `NVDS_Stabilizer.pth`.
  4. Export fixed-size ONNX models.
  5. Integrate only the exported ONNX into this project.

## NVDS Model Shape And Placement

- `reference/NVDS/full_model.py` defines `NVDS(nn.Module)`.
- Forward input is conceptually `[B, 4, 4, H, W]`:
  - batch,
  - 4-frame temporal window,
  - 4 channels = RGB + initial depth/disparity,
  - inference height/width.
- Forward output is `[B, 1, H, W]`, a stabilized target-frame depth/disparity.
- The model uses `mit_b5()` backbone and changes the first conv from 3 to 4
  channels.
- It is a postprocessor, not a DA3 replacement:
  `RGB frame + DA3 depth/near sequence -> stabilized depth/near -> stereo renderer`.
- Best first integration point:
  - after DA3 depth inference in `offline/two_dvr.py` / `offline/two_dvr_pynv.py`,
  - before `renderer.render(...)` or `renderer.prepare_near_gpu(...)`.
- For the CPU/ffmpeg path, feed `renderer.render(frame, stable_depth_or_near)`.
- For the PyNv path, add an ONNX stabilizer step before
  `renderer.prepare_near_gpu(...)`; this is feasible but more invasive because
  the path currently keeps RGB/depth mostly on GPU and only round-trips DA3
  input/output through ORT.

## Export Risks

- Export should be fixed-resolution first. Dynamic H/W is high risk due to
  Python shape arithmetic, padding branches, window partitioning, and many
  `view/reshape` operations in `stabilization_attention.py`.
- The largest ONNX risk is in-place slice updates such as:
  `attn[:, :, ...] = attn[:, :, ...] + bias`.
  Newer PyTorch exporters may lower some of these to Scatter/Where-style ONNX
  ops, but PyTorch 1.9 exporter is likely weaker.
- Recommended first export resolution:
  - proof: `384x384`,
  - real 16:9 offline test: `672x384` (closest multiple-of-32 width for height
    384),
  - optional higher quality: `896x512` or other fixed aspect models only after
    memory/perf is measured.
- README states the stabilizer alone uses about 5GB GPU memory at `896x384` on
  RTX-A6000. This is not a realtime candidate in this project. Treat it as
  offline-only.
- The checkpoint is loaded in the official script into `torch.nn.DataParallel`,
  so export code may need to strip `module.` prefixes or load into DataParallel
  first and export `model.module`.

## Input Normalization Risk

- Official inference normalizes RGB with ImageNet mean/std.
- Official inference normalizes the initial depth/disparity per frame to `[0,1]`
  before concatenating it with RGB.
- Current DA3 output is distance-like depth, and the renderer converts it to
  normalized near/disparity by reciprocal + percentile band.
- Because NVDS was trained on DPT/MiDaS-style relative inverse depth/disparity,
  the safer first experiment is to feed NVDS a normalized near/disparity map,
  not raw DA3 distance depth.
- If output is treated as near/disparity, renderer integration should bypass the
  reciprocal step or add a dedicated `render_from_near(...)` path. If output is
  converted back into distance-like depth, extra inversion and rescaling are
  needed. This needs A/B validation.

## Recommended Next Step

- Do not add PyTorch to the main project.
- Add an export-only script under `tools/` or `examples/` after user approval,
  and run it in a separate Python 3.8/3.9 environment.
- First deliverable:
  - `models/NVDS/NVDS_Stabilizer_672x384.onnx`,
  - a small ORT wrapper `offline/nvds_stabilizer.py`,
  - CLI flag for offline only, e.g. `--depth-stabilizer nvds`.
- Keep realtime/default VVPS path unchanged until NVDS quality and performance
  are proven.

## NVDS 384x384 ONNX Export Review

- User added:
  - `examples/export_nvds_onnx.py`
  - `models/NVDS/NVDS_Stabilizer_384x384.onnx`
  - `models/NVDS/NVDS_Stabilizer_384x384.onnx.json`
- Static ONNX validation passed in the project `.venv`:
  - input: `rgbd_seq`, float32, `[1, 4, 4, 384, 384]`
  - output: `stabilized_depth`, float32, `[1, 1, 384, 384]`
  - opset: 17
  - graph nodes: 16,757
  - initializers: 1,210
  - domains: only standard `ai.onnx`
  - no `ATen` nodes
  - no custom-domain nodes
  - `onnx.checker.check_model(...)` passed
  - ONNX Runtime CPU session loads successfully.
- Metadata JSON from the export environment reports an ORT CUDA comparison:
  - output shape `[1, 1, 384, 384]`
  - `max_abs_diff ~= 3.49e-05`
  - `mean_abs_diff ~= 4.10e-06`
- Conclusion:
  - `384x384` is good enough for export validation and a first ORT wrapper
    smoke test.
  - It is not the recommended final 16:9 offline model because it discards or
    distorts aspect detail unless the wrapper deliberately letterboxes. Export a
    `672x384` model next for normal 16:9 footage.
- Script adjustment made:
  - `--output` now defaults to
    `models/NVDS/NVDS_Stabilizer_{width}x{height}.onnx`, so a `--width 672
    --height 384` export no longer writes a misleading `384x384` filename.
  - Added compatibility around `torch.onnx.export(..., dynamo=False)` so older
    PyTorch builds that do not expose the `dynamo` keyword can still run.
  - Added metadata fields for layout, sequence length, target frame, RGB
    preprocessing, and depth/near preprocessing.
- Syntax-only check of the edited script passed via in-memory `compile(...)`.
  `py_compile` could not be used because writing `examples/__pycache__` was
  denied by filesystem permissions.

## Offline 2DVR NVDS UI And Runtime Integration

- User exported the 16:9 ONNX:
  - `models/NVDS/NVDS_Stabilizer_672x384.onnx`
  - `models/NVDS/NVDS_Stabilizer_672x384.onnx.json`
- Static validation in the project `.venv` passed:
  - input: `rgbd_seq`, float32, `[1, 4, 4, 384, 672]`
  - output: `stabilized_depth`, float32, `[1, 1, 384, 672]`
  - opset: 17
  - standard `ai.onnx` domain only
  - no `ATen`
  - `onnx.checker.check_model(...)` passed
- Added `offline/nvds_stabilizer.py`:
  - ONNX Runtime-only wrapper.
  - Maintains causal 4-frame RGBD history.
  - Converts DA3 distance depth to normalized near/disparity before feeding
    NVDS.
  - Returns normalized stable near/disparity for rendering.
  - Tracks per-frame NVDS inference time and reports FPS via `fps_summary()`.
  - Supports the same ORT TensorRT EP cache style as DA3 under
    `runtime_cache/nvds_trt/NVDS_Stabilizer_672x384`.
- Added direct near/disparity rendering:
  - `offline/two_dvr_render.py:StereoRenderer.render_near(...)`
  - `offline/two_dvr_gpu.py:GpuStereoRenderer.render_near(...)`
  - This prevents NVDS output from being reinterpreted as raw DA3 distance depth.
- Added CLI flag:
  - `--depth-stabilizer default`
  - `--depth-stabilizer nvds`
  - `nvds` is limited to 16:9 processing sizes and disables the PyNv hot path
    for now. It still may use GPU rendering through the CuPy renderer.
- Added `offline/two_dvr.py build-trt --include-nvds`.
  - Current check showed ORT providers include
    `TensorrtExecutionProvider`, `CUDAExecutionProvider`, `CPUExecutionProvider`.
  - Current NVDS TRT cache state was `False` before building.
  - To measure FPS without first-run TRT build overhead:
    `.\.venv\Scripts\python.exe offline\two_dvr.py build-trt --model both --include-nvds`
- Added UI setting:
  - `ui/settings.py`: `two_dvr_depth_stabilizer`, default `default`.
  - `ui/pages/two_dvr_page.py`: under "画质速度/Quality speed", add
    "时序稳定/Temporal stability" with:
    - default algorithm
    - NVDS model (16:9 only)
  - Both single and batch tabs share the same setting.
- Added i18n keys in:
  - `ui/translations/zh_CN.json`
  - `ui/translations/en_US.json`
- Validation:
  - JSON translation parse passed.
  - Syntax-only compile passed for changed Python files.
  - Tests:
    `.\.venv\Scripts\python.exe -m pytest -p no:cacheprovider tests\test_two_dvr_temporal_stability.py tests\test_da3_download_and_trt.py tests\test_two_dvr_progress.py -q`
    -> `32 passed`.

## nunif iw3 Depth Stabilization Reference

- User asked what depth stabilizer `reference\nunif` uses for 2D-to-3D.
- Relevant tool is `reference\nunif\iw3`.
- It does not use NVDS as a depth stabilizer.
- Main video-depth stabilization is `Flicker Reduction` / `--ema-normalize`:
  - Implemented by `reference\nunif\iw3\depth_scaler.py:EMAMinMaxScaler`.
  - It tracks per-frame/minibatch depth min/max, optionally with a lookahead
    window, then smooths min/max by exponential moving average before
    min-max normalization.
  - CLI params are `--ema-normalize`, `--ema-decay` default `0.75`, and
    `--ema-buffer` default `30`.
  - README recommends decay around `0.75` to `0.99`; buffer 30 means roughly
    1 second lookahead at 30 FPS.
- Scene cut handling:
  - `--scene-detect` uses TransNetV2 first.
  - At scene boundaries, nunif resets VideoDepthAnything state and Flicker
    Reduction / Min-Max normalization state.
- VDA path:
  - `VideoDepthAnything` / `VideoDepthAnythingStreaming` are video depth models
    with their own temporal model state.
  - nunif still recommends combining them with `--ema-normalize` and
    `--scene-detect`.
- `row_flow_v3` is the default stereo generation method. It is for learned
  backward warping / stereo synthesis, not depth temporal stabilization.
- Real-time/desktop path in `reference\nunif\iw3\desktop` enables Flicker
  Reduction by default but forces `buffer_size=1`, avoiding lookahead latency.

## 2DVR Default Stabilizer vs nunif Reference

- Current built-in 2DVR stabilizer already overlaps with nunif's main idea:
  - `offline/two_dvr_render.py:_normalize_near(...)` converts DA3 distance to
    inverse-depth/near and uses the 5/95 percentile band.
  - `TemporalDepthStabilizer.normalization_band(...)` EMA-smooths that band.
  - Our alpha maps to nunif decay as `decay = 1 - alpha`; current defaults are
    norm alpha `0.10` (decay `0.90`) and depth/base alpha `0.20` (decay `0.80`),
    both inside nunif's suggested `0.75..0.99` decay range.
- Current built-in stabilizer goes beyond nunif:
  - base/detail split on normalized near/disparity;
  - only smooths low-frequency base, always reinjects current detail;
  - optional global translation motion compensation;
  - affine scale/bias match;
  - evidence gate to let moving/disoccluded regions follow current frame.
- Existing offline lookahead:
  - `SymmetricBaseWindow` exists in CPU code, and `GpuSymmetricWindow` exists in
    GPU code.
  - PyNv path uses `GpuSymmetricWindow` when `--temporal-window > 0` and disables
    causal temporal-depth smoothing there.
  - The ordinary ffmpeg/CPU pipeline currently does not consume
    `--temporal-window`, so offline lookahead is not consistently applied across
    paths.
- Main borrowable nunif ideas:
  - Add lookahead/symmetric smoothing for the normalization band itself, not only
    the base/near map. This is closest to nunif `--ema-buffer`.
  - Add scene-cut detection/reset for 2DVR. The repo already has
    `utils.scene_detection.SceneCutDetector`, but 2DVR does not currently use it.
    Reset should clear renderer temporal state, NVDS state when selected, and any
    symmetric window buffer at cut boundaries.
  - Avoid using NVDS as the default path unless performance is solved; nunif's
    stabilizer is algorithmic EMA/range reset, not a separate heavy model.

## NVDS TRT Attempt Result And Crash Fix

- User provided a failed offline NVDS run log.
- Findings:
  - DA3 TensorRT logs a UINT8 `Transpose` parse error for the folded uint8
    preprocessing node. This is the known DA3 partition behavior: preprocessing
    falls back outside TRT while the supported DA3 trunk can still use TRT.
  - NVDS TensorRT is not viable with the current exported graph:
    - ORT/TensorRT reported an optimized protobuf size over 2GB.
    - ORT then reported: `No graph will run on TensorRT execution provider`.
    - The graph contains many `ScatterND` nodes; CUDA EP can load it, but TRT EP
      cannot provide a useful cached engine in this form.
  - Actual process crash was unrelated to NVDS math:
    `_encode_proc(...)` passed an integer bitrate into `subprocess.Popen`, causing
    `TypeError: expected str, bytes or os.PathLike object, not int`.
- Fixes applied:
  - `offline/two_dvr.py:_encode_proc(...)` now converts `preset` and `bitrate`
    to strings before building the ffmpeg command.
  - `offline/nvds_stabilizer.py` now disables NVDS TensorRT by default
    (`NVDS_TRT_SUPPORTED = False`).
  - Requesting NVDS with `--provider trt` now maps to
    `CUDAExecutionProvider -> CPUExecutionProvider` for NVDS only. DA3 still uses
    TensorRT normally.
  - `build-trt --include-nvds` now performs a CUDA warmup for NVDS and logs that
    NVDS TensorRT cache is disabled, instead of attempting a doomed TRT build.
- Verification after the fix:
  - `onnx_providers("trt")` for NVDS returns
    `['CUDAExecutionProvider', 'CPUExecutionProvider']`.
  - `trt_engine_cached()` for NVDS returns `False`.
  - Syntax-only compile passed for `offline/nvds_stabilizer.py` and
    `offline/two_dvr.py`.
  - Tests:
    `.\.venv\Scripts\python.exe -m pytest -p no:cacheprovider tests\test_two_dvr_temporal_stability.py tests\test_da3_download_and_trt.py tests\test_two_dvr_progress.py -q`
    -> `32 passed`.

## NVDS Freeze Root Cause And VRAM Fix (commit 28499d5)

- User reported NVDS run getting progressively slower (`1.9 -> 2.1 -> 1.2 fps`)
  then freezing. Reproduced with a live `nvidia-smi` memory trace.
- Root cause is NOT "NVDS too slow": it is VRAM exhaustion. DA3 (TRT) + NVDS
  (CUDA EP) + CuPy renderer share the GPU; real working set is ~14.9 GB / 16 GB.
  ORT's default arena uses `kNextPowerOfTwo` and never frees, so combined usage
  creeps past physical VRAM and Windows WDDM spills to shared system RAM -> the
  progressive slowdown ending in an effective freeze. Serial mode was worse
  (0.1 fps) because it relieved no memory pressure.
- Fix in `offline/nvds_stabilizer.py`: give the NVDS `CUDAExecutionProvider` a
  `gpu_mem_limit` auto-derived as `total VRAM - _NVDS_VRAM_RESERVE_MB` (default
  reserve 4096 MB). Keep ORT's fast default arena/cudnn kernels -- a first
  attempt using `kSameAsRequested` + `cudnn_conv_algo_search=DEFAULT` also stopped
  the freeze but cut fps from 2.8 to 0.4, so it was reverted.
- Env overrides: `PTMS_NVDS_GPU_MEM_LIMIT_MB`, `PTMS_NVDS_VRAM_RESERVE_MB`.
- Measured on the 16 GB card: monolith 672x384 went from `1.2 fps -> freeze` to a
  stable `2.8 fps`, VRAM flat at ~12.6 GB.

## NVDS Backbone/Head Split (commit 02fe09d)

- `examples/export_nvds_onnx.py --split` exports two graphs:
  - `NVDS_Backbone_{w}x{h}.onnx`: single RGBD frame `[1,4,H,W]` -> 4 feature maps.
  - `NVDS_Head_{w}x{h}.onnx`: 4 window frames' features (stacked on batch) +
    last-frame RGB `[1,3,H,W]` -> stabilized depth `[1,1,H,W]`.
- `offline/nvds_stabilizer.py` auto-detects the split graphs, runs the backbone
  once per frame, caches the last 4 feature tuples, runs the head per frame.
  Falls back to the monolith when split files are absent.
- Numerically identical to the monolith (backbone has no cross-frame ops, so its
  features are batch-independent): `max_abs_diff ~= 3.5e-05`; the export script
  verifies split-vs-monolith in one run.
- IMPORTANT finding that corrected the earlier assumption: the backbone is NOT
  the bottleneck. Measured at 672x384 (CUDA EP, 40-frame mean):
  - backbone (1 frame) = 26 ms, head (focal cross-attention) = 208 ms.
  - monolith NVDS = 293 ms, split NVDS = 258 ms -> only 1.13x.
  The focal-attention head dominates and cannot be de-duplicated (it is the
  per-output-frame cross-frame fusion). Microbench tool:
  `tools/nvds_split_bench.py [split|mono]`.

## NVDS Resolution Tiers, Default 512x288 (commit bd4ebe3, UI trim bc30dcf)

- Head cost scales with stride-4 token count (input pixels), so resolution is the
  real lever. Measured split: 672x384 = 3.9 NVDS fps; 512x288 = 6.8 (1.76x),
  numerically consistent (`max_abs_diff ~= 2.6e-05`). End-to-end 512x288 split run
  reached ~4.2 fps, VRAM ~11.5 GB.
- `offline/nvds_stabilizer.py` parametrized by resolution: `NVDS_RESOLUTIONS`
  (`512x288` default, `672x384` hi-q), `resolve_resolution`,
  `resolve_available_resolution` (falls back to an installed tier),
  per-resolution model path helpers.
- `offline/two_dvr.py`: added `--nvds-res 512x288|672x384`;
  `_create_depth_stabilizer` loads the chosen tier and logs `model=<res>`.
- UI (commit bc30dcf, per user request): the "Temporal stability" dropdown keeps
  only one NVDS entry, labeled `NVDS模型(限16x9,>16G VRAM)` / `NVDS model (16:9
  only, >16G VRAM)`, which uses the 512x288 default. The 672x384 tier was removed
  from the UI but stays reachable via `--nvds-res 672x384`.

## DA3 Large 1036 Model (commit 6b30405)

- `examples/da3_to_onnx.py` gained a `large` variant (ViT-L). Exported
  `models/DA3/da3_large_1036.onnx` (1.35 GB) from the Large weights in
  `models/DA3/Large/model.safetensors` using the toolbox torch env:
  `... examples/da3_to_onnx.py --variant large --size 1036 --src-root models/DA3 --out-dir models/DA3 --device cuda`.
  Validated rel error `5.9e-05`.
- `offline/da3_depth.py`: `DA3_PRESETS["large_hd"] -> da3_large_1036.onnx`.
- `offline/two_dvr.py`: `large_hd` added to `--model`, `build-trt`, `download`
  choices.
- UI depth-model dropdown now: `Base / Base高清(1036) / Large高清(1036)`; the two
  Small entries were removed; default Base.

## README Docs (commit 26606c2)

- `examples/da3_README.md` updated for the Large (ViT-L) tier and its export.
- Added `examples/nvds_README.md` (same format) documenting the NVDS exporter,
  monolith vs split modes, resolution tiers, IO signatures, preprocessing,
  inference example, and the TensorRT-unusable note. Upstream:
  `https://github.com/RaymondWang987/NVDS`.

## Pre-Run Model Download Dialog (commit aee73cd)

- New `utils/hf_download.py`: mirror-aware HF helpers -- `hf_endpoints(language)`
  (zh -> hf-mirror.com first, `HF_ENDPOINT` overrides), `remote_size(urls)`
  (Content-Length probe for the confirmation), `download_file(urls, dest,
  progress, log)` (`.part` temp + atomic rename + mirror fallback).
- `offline/nvds_stabilizer.py`: NVDS HF repo `zerochocobo/NVDS_onnx`,
  `required_filenames`/`missing_filenames`/`download_targets` (per-resolution
  backbone+head). `offline/da3_depth.py`: `download_target(model, language)`.
- New `ui/widgets/model_download_dialog.py`: lists each missing file with its
  size and a total, waits for confirm, downloads sequentially with an overall
  progress bar; `exec()` returns Accepted only on full success.
- `ui/pages/two_dvr_page.py`: `run_single`/`run_batch` call `_preflight_models`
  before launching the subprocess; missing DA3 model (or NVDS files when NVDS is
  selected) opens the dialog. Cancel or download failure aborts the run.
- Added `modeldl.*` strings (en/zh) and `tests/test_model_download.py`.
- NOTE for next session: NVDS downloads only succeed once
  `NVDS_Backbone_512x288.onnx` + `NVDS_Head_512x288.onnx` are uploaded to
  `zerochocobo/NVDS_onnx`; DA3 `large_hd` similarly needs uploading to
  `zerochocobo/DepthAnything3_ONNX`. Until then the dialog shows a clear failure.

## Validation Summary For This Session

- `examples/export_nvds_onnx.py --split` split-vs-monolith check: `~3.5e-05`.
- `tools/nvds_split_bench.py`: confirmed head is the NVDS bottleneck.
- DA3 Large export validation: rel error `5.9e-05`.
- Tests last run green:
  `tests\test_model_download.py tests\test_ui_smoke.py tests\test_two_dvr_ui_hole_fill.py tests\test_two_dvr_temporal_stability.py tests\test_da3_download_and_trt.py tests\test_trt_cache_dialog.py tests\test_two_dvr_progress.py`
  -> `51 passed`.
- Open working-tree items left for the user to resolve (not changed by this
  session's commits): `models/NVDS/NVDS_Stabilizer_672x384.onnx.json` and
  `models/NVDS/NVDS_split_672x384.json` show as deleted; decide whether to restore
  (`git checkout -- models/NVDS/*.json`) or commit their removal.

## DLNA Live Time Index Directory

- User requested DLNA Live entries to always be directories, including videos
  shorter than the old chapter-container threshold.
- Updated `dlna/content_directory.py`:
  - bumped DIDL schema/object id version to `ptv10` so DLNA clients can refresh
    cached browse trees;
  - Live passthrough entries now always expose `pl_`/`pla_`/`pl3_` containers;
  - each Live container now includes a `[Select Time Index]_...` directory plus
    the existing quick chapter file links;
  - added nested time-index object ids:
    - `lix_`: Select Time Index root;
    - `lig_`: 10-minute group;
    - `lim_`: 1-minute selection;
    - `li5_`: 5-second playback links;
  - Select Time Index root skips the 10-minute group layer when there is only
    one group, directly showing minute folders;
  - time labels use `MM:SS` by default and `H:MM:SS` for videos >= 1 hour;
  - final playback links still use `/passthrough_live/...?...t=<seconds>` so
    clicking starts the live stream at that offset.
- Added basic CDS title i18n:
  - `http_app/routes_dlna.py` reads `Accept-Language`;
  - supported labels are English, Simplified Chinese, and Japanese;
  - if the DLNA frontend sends no language header, the title falls back to
    English.
- Tests updated/added:
  - short Live is now asserted as a directory;
  - Select Time Index title language;
  - 10-minute group, 1-minute, 5-second link generation;
  - single-group layer skipping;
  - hour-format labels;
  - versioned time-index object id parsing;
  - `ptv10` expectations for related DLNA/SI tests.
- Validation:
  - `.\.venv\Scripts\python.exe -m py_compile dlna\content_directory.py http_app\routes_dlna.py` passed.
  - `.\.venv\Scripts\python.exe -m pytest tests\test_content_directory_modes.py -q`
    -> `40 passed`.
  - `.\.venv\Scripts\python.exe -m pytest tests\test_si_mix.py -q`
    -> `14 passed`.
  - `.\.venv\Scripts\python.exe -m pytest tests\test_player_compat.py::PlayerCompatProfileTests::test_cds_ui_language_prefers_accept_language_quality -q`
    -> `1 passed`.
  - Full `tests\test_player_compat.py` still hits an unrelated
    `PermissionError` opening
    `debug_output/request_history/request_history_20260620.jsonl` during
    TestClient shutdown.

## DLNA Live Directory Mode Prefix

- User requested DLNA Live directories to show mode prefixes:
  - green-screen passthrough: `[GREEN]`
  - alpha passthrough: `[ALPHA]`
- Updated `dlna/content_directory.py` so the prefix is applied only to directory
  titles:
  - top Live container title, e.g. `[GREEN]_movie_passthrough_live`;
  - Select Time Index directory, e.g. `[选择时间索引]_[GREEN]_movie_passthrough_live`;
  - 10-minute group directories;
  - 1-minute directories.
- Final playable file links, including quick chapter items and 5-second points,
  keep their existing time-prefixed filenames without the new mode prefix.
- Validation:
  - `.\.venv\Scripts\python.exe -m py_compile dlna\content_directory.py` passed.
  - `.\.venv\Scripts\python.exe -m pytest tests\test_content_directory_modes.py -q`
    -> `40 passed`.

## DLNA Seek Entry Default Off

- User reported DLNA directories still showing the experimental
  `/passthrough_seek` entry after testing.
- Current saved UI settings had:
  - `passthrough_seek_enabled: true`
  - `passthrough_seek_dlna: true`
- Updated local `runtime_cache/ui_settings.json` to set
  `passthrough_seek_dlna: false`, leaving `passthrough_seek_enabled` unchanged
  so direct route testing can still be used without DLNA advertisement.
- Added a one-time UI settings migration in `ui/settings.py`:
  `20260620_seek_dlna_default_off`; legacy saved configs with
  `passthrough_seek_dlna=true` now load as false by default.
- Validation:
  - Syntax check via in-memory `compile(...)` for `ui/settings.py` passed.
  - Targeted settings tests passed:
    `test_legacy_seekable_passthrough_dlna_migrates_off`,
    `test_server_env_omits_blank_subtitle_color`,
    `test_server_env_can_enable_seekable_passthrough_for_ui_start`.
  - Full `tests\test_settings.py` currently has an unrelated stale failure:
    `test_passthrough_mode_mapping` expects default `all`, while current
    defaults include `mode_two_dvr=True`, producing `green,alpha,two_dvr`.

## 2DVR Skip Existing Label Fix

- User reported the 2DVR checkbox text showed "如果透视文件存在就忽略" but should
  say "如果目标文件存在就忽略".
- The checkbox uses the shared i18n key `offline.skip_existing`.
- Updated:
  - `ui/translations/zh_CN.json`: `如果目标文件存在就忽略`
  - `ui/translations/en_US.json`: `Skip if target file already exists`
  - `ui/translations/ja_JP.json`: `ターゲットファイルが存在する場合はスキップ`
- Validation:
  - JSON parse with `utf-8-sig` for all translation files passed.
  - `tests\test_i18n.py` still has an unrelated existing failure because
    `ja_JP.json` is missing keys such as `modeldl.*` and
    `twodvr.temporal_*`.

## README And CHANGELOG Documentation Update

- Updated all three README language variants:
  - `README.md`
  - `README.zh-CN.md`
  - `README.ja-JP.md`
- Added user-facing documentation for:
  - realtime and offline 2D-to-3D / VR generation;
  - DA3 depth, HD/Large DA3, and optional NVDS offline temporal stabilization;
  - simultaneous interpretation playback through same-stem `.si.wav` sidecars
    and `[SI]` DLNA entries;
  - DLNA Live time-index directories and `[GREEN]` / `[ALPHA]` directory
    prefixes;
  - `two_dvr`, `PT_TWO_DVR_*`, `PT_SI_*`, and `/media_si` configuration notes.
- Updated `CHANGELOG.md` with missing English and Chinese entries for
  2026-06-15 through 2026-06-20, based on the recent HANDOVER documents.
- Validation:
  - `git diff --check -- README.md README.zh-CN.md README.ja-JP.md CHANGELOG.md`
    passed with only Git's existing LF/CRLF conversion warnings.
  - `README.md`, `README.zh-CN.md`, `README.ja-JP.md`, and `CHANGELOG.md`
    read successfully with `encoding="utf-8-sig"`.
  - `rg` found no stale `VideoDepthAnything` NVDS link and no incorrect
    Japanese time-index label.

## Website Homepage Version And Feature Cards

- Updated `website/index.html` release information:
  - version: `v1.0.0`
  - release date: `2026-6-21`
- Added three feature cards after Desktop control and Lighting:
  - realtime 2D to 3D;
  - simultaneous interpretation;
  - VR player filename compatibility.
- Updated `website/script.js` translations for the new cards in English,
  Simplified Chinese, and Japanese.
- Validation:
  - `rg` confirmed no `v0.1.0` / `2026-6-1` residue in the website files.
  - A UTF-8-sig Python check confirmed every `data-i18n` key in
    `website/index.html` has entries in all three language maps.
  - `git diff --check -- website\index.html website\script.js` passed with
    only Git's existing LF/CRLF conversion warnings.

## Passthrough Seek VMP4 Slot Review Fixes

- User asked to validate external code-review feedback before more real-device
  testing.
- Reviewed feedback from:
  `C:\Users\dennis\.codex\attachments\14e5f25c-de02-41ed-acb1-6e6237becbf2\pasted-text.txt`.
- Important observation from `debug_output/server.log`: the tail of that log does
  not look like the current VMP4 slot path. It shows old
  `passthrough_seek ... backend=pynv_hevc` streaming responses and ffmpeg fMP4 mux
  commands, not `VMP4 slot response`. Next real-device run should confirm the
  new process logs `VMP4 slot response` and returns
  `X-Passthrough-VMP4-Backend: slot`.

Completed fixes:

- Added `PT_PASSTHROUGH_SEEK_VMP4_SLOT_READY_ONLY=1` default in `config.py`.
- Added GUI launch env:
  `PT_PASSTHROUGH_SEEK_VMP4_SLOT_READY_ONLY=1` in `ui/settings.py`.
- Slot GET ready-only behavior in `http_app/routes_media.py`:
  - If a requested media slot is not ready, schedule the slot build and return
    `503 VMP4 slot not ready` with `Retry-After: 1`.
  - HEAD is allowed because it has no body.
  - Open ranges such as `bytes=0-` are bounded before the next unready slot, so
    the server does not stream placeholder bytes for future slots.
  - Added diagnostics:
    `X-Passthrough-VMP4-Slot-Ready-Only` and
    `X-Passthrough-VMP4-Slot-Ready-Bounded`.
- Added route-level VMP4 slot layout cache:
  - Limit: 64 entries.
  - Key includes source path/stat, duration, fps, GOP, max sample bytes, output
    stsd hash, placeholder payload hash, codec, NAL length size, and output size.
- Reduced manifest write churn:
  `ensure_vmp4_slot_manifest(...)` writes only when the manifest content changes,
  ignoring `updated_at` differences.
- Fixed manifest atomic writes in `pipeline/passthrough_vmp4_slot.py`:
  - Unique temp file via `tempfile.mkstemp`.
  - Per-target write lock.
  - Windows `os.replace` PermissionError retry.
- Fixed slot payload writes:
  - Placeholder and HEVC Annex-B payload writers now use atomic temp + replace.
  - Placeholder build worker no longer uses a fixed `.tmp` payload name.
- Fixed zero-byte payload state:
  - `payload_size == 0` becomes `failed`, reason `payload-empty`.
  - Ready requires `0 < payload_size <= sample_size`.
- Refactored `iter_vmp4_slot_range(...)` payload accounting:
  - Explicit `file_payload_size` and `emitted_payload_size`.
  - Filler range now depends on actual emitted payload length.
- Added `layout-moov-offsets-not-converged` guard if the moov/mdat offset loop
  fails to converge in four iterations.

Files changed for this slice:

- `config.py`
- `ui/settings.py`
- `http_app/routes_media.py`
- `pipeline/passthrough_vmp4_slot.py`
- `tests/test_passthrough_vmp4_slot.py`
- `tests/test_config_defaults.py`
- `tests/test_settings.py`
- `summary/summary_20260619_PASSTHROUGH_SEEK_REWORK_PLAN_CN.md`
- `prompt/HANDOVER_20260620.md`

Validation:

- `.\.venv\Scripts\python.exe -m pytest -p no:cacheprovider tests\test_passthrough_vmp4_slot.py -q`
  -> `12 passed`.
- `.\.venv\Scripts\python.exe -m pytest -p no:cacheprovider tests\test_passthrough_vmp4_slot.py tests\test_routes_media_cache.py tests\test_config_defaults.py tests\test_settings.py tests\test_content_directory_modes.py -q`
  -> `138 passed, 8 subtests passed`.
- `$env:PYTHONPYCACHEPREFIX='debug_output\pycache_compile'; .\.venv\Scripts\python.exe -m py_compile config.py ui\settings.py http_app\routes_media.py pipeline\passthrough_vmp4_slot.py tests\test_passthrough_vmp4_slot.py tests\test_config_defaults.py tests\test_settings.py`
  -> passed.

Remaining work before meaningful real-device slot validation:

- Placeholder builder still writes the HEVC placeholder sample, not a real
  passthrough GOP.
- Next implementation target is a video-only GPU GOP slot builder:
  slot start time -> PyNv HEVC Annex-B GOP -> `write_vmp4_slot_hevc_annexb_payload`
  -> ready manifest state.
- A true phase-1 test should use multi-frame GOP payloads, because one sample per
  slot / one placeholder frame does not prove the final playback behavior.

## Quest3 Slot Follow-Up: no-Range Fix And Real GOP Builder

User reported Quest3 player behavior:

- Skybox: cannot play.
- HereSphere: blank video.
- 4XVR: stuck.
- Asked to inspect `debug_output/server.log`.

Log findings:

- Newer log lines confirm the slot backend was active:
  `VMP4 slot request`, `VMP4 slot response`,
  `source_codec=av01 output_codec=hevc`.
- The important HTTP bug was no-Range GET returning partial `206`, e.g.
  `range=None content_range='bytes 0-.../...'`.
- That explains Skybox-style startup failure: a request without `Range` must not
  be answered as partial content.
- HereSphere blank video was still expected while the slot builder wrote the
  one-frame HEVC placeholder payload.
- 4XVR logs mixed `/passthrough_live` and `/passthrough_seek`; the seek-side
  result was not meaningful while placeholder/no-Range issues existed.

Completed after this handoff:

- `http_app/routes_media.py`
  - Ready-only no-Range GET now schedules the blocking slot and returns
    `503 VMP4 slot not ready` with `Retry-After: 1` instead of an invalid
    partial `206`.
  - Open Range requests can be bounded before the next unready slot.
  - Added next-slot diagnostics:
    `X-Passthrough-VMP4-Next-Slot`,
    `X-Passthrough-VMP4-Next-Slot-State`,
    `X-Passthrough-VMP4-Next-Slot-Build`.
  - Slot worker no longer writes placeholder payloads for ready slots.
  - Slot worker now calls the PyNv GOP builder, writes the HEVC Annex-B result
    through `write_vmp4_slot_hevc_annexb_payload(...)`, and marks the manifest
    ready only on a valid non-empty payload.
  - After a local MP4 smoke showed FFmpeg warning when multiple access units
    were stored inside one MP4 sample, the worker now asks the builder for one
    IDR access unit per slot (`frame_count=1`). This matches the current
    one-slot/one-sample layout. Smooth multi-frame slot playback needs a later
    frame-level sample table.
  - Build failures mark the slot `failed`; there is no placeholder fallback.
- `pipeline/pynv_stream.py`
  - Added `build_passthrough_hevc_annexb_gop(...)`.
  - It reuses the live PyNv path for green/alpha:
    PyNv decode -> Matter/AlphaPacker -> NVENC HEVC Annex-B.
  - The first slot frame forces IDR plus parameter sets.
  - The slot builder derives a bounded NVENC bitrate from the slot sample byte
    budget to avoid immediate payload oversize.
  - `two_dvr` slot build is explicitly unsupported for now.
- `config.py`
  - Updated the stale `PT_PASSTHROUGH_SEEK_VMP4_SLOT_BUILD_PLACEHOLDER` comment:
    the env name is legacy; the default enabled behavior now means real slot
    payload build.
- `tests/test_passthrough_vmp4_slot.py`
  - Added/updated tests for no-Range ready-only behavior.
  - Added mock Annex-B slot build coverage.
  - Added failure coverage proving builder errors do not fall back to
    placeholder payloads.
- `summary/summary_20260619_PASSTHROUGH_SEEK_REWORK_PLAN_CN.md`
  - Added sections 21 and 22 for this work.

Validation after real GOP builder changes:

- `.\.venv\Scripts\python.exe -m pytest -p no:cacheprovider tests\test_passthrough_vmp4_slot.py -q`
  -> `14 passed`.
- `.\.venv\Scripts\python.exe -m pytest -p no:cacheprovider tests\test_passthrough_vmp4_slot.py tests\test_routes_media_cache.py tests\test_config_defaults.py tests\test_settings.py tests\test_content_directory_modes.py -q`
  -> `140 passed, 8 subtests passed`.
- `$env:PYTHONPYCACHEPREFIX='debug_output\pycache_compile'; .\.venv\Scripts\python.exe -m py_compile config.py ui\settings.py http_app\routes_media.py pipeline\passthrough_vmp4_slot.py pipeline\pynv_stream.py tests\test_passthrough_vmp4_slot.py tests\test_config_defaults.py tests\test_settings.py`
  -> passed.
- Local GPU smoke:
  - Initial inline `.\.venv\Scripts\python.exe` CUDA smokes timed out. After
    rereading `PROJECT.md`, this was identified as the wrong launch shape.
  - Correct smoke launch must run `apply_runtime_dll_paths()` and
    `configure_gpu_runtime_cache()` before any CUDA/CuPy/ORT import, and should
    use `uv run python`.
  - CUDA self-check passed:
    `sys.executable=...\.venv\Scripts\python.exe`,
    `nvrtc=(12, 9)`, `CUPY_COMPILE_WITH_PTX='0'`,
    `compiler._use_ptx=False`,
    `arch=('-arch=sm_120', 'cubin')`,
    `CUPY_CACHE_DIR=runtime_cache\cupy`.
  - Minimal CuPy/NVENC encode smoke passed:
    `CreateEncoder(...)` succeeded and `EndEncode()` emitted 183 bytes.
  - Narrow slot builder smoke passed with `Matter(load_model=False)` and
    `PT_PASSTHROUGH_RVM_BYPASS_ALPHA=1`: 2 frames emitted 227146 bytes of HEVC
    Annex-B beginning with a VPS NAL.
  - Full `Matter()` / CUDA RVM slot builder smoke passed:
    RVM active providers were `['CUDAExecutionProvider', 'CPUExecutionProvider']`,
    `rvm_iobinding=True`, and 1 frame emitted 90888 bytes of HEVC Annex-B.
  - Real PyNv slot MP4 smoke passed:
    generated `debug_output/vmp4_slot_real_pynv_smoke.mp4` and
    `debug_output/vmp4_slot_real_pynv_slot0.bin`; payload was 90888 bytes inside
    a 390366-byte sample. `ffprobe` saw `codec_name=hevc`,
    `codec_tag_string=hvc1`, `width=720`, `height=1280`, `nb_frames=8`.
    `ffmpeg -v error -i ... -frames:v 2 -f null -` completed without errors.

Remaining risks before/after next Quest test:

- Template `hvcC` still comes from the generated template MP4, while actual slot
  GOP bytes now come from PyNv/NVENC. Because the GOP includes VPS/SPS/PPS this
  may work, but strict players may require a PyNv-derived `hvcC`.
- The virtual MP4 layout is still one MP4 sample per GOP slot. If Quest players
  play the new one-IDR-per-slot output but it is not smooth, the next structural
  change is frame-level samples inside each slot window.
- On the next run, confirm logs show both:
  `slot PyNv GOP ready` and `passthrough_seek VMP4 slot build ready`.

## Slot Builder Hardening Before Real-Device Test (R1/R2/R3)

Follow-up to the review. The real PyNv slot builder had three real-device risks;
fixed before the next Quest run.

- R1 (4K/8K single-IDR oversize -> permanent slot failure):
  - New `PT_PASSTHROUGH_SEEK_VMP4_SLOT_IDR_BPP` (default 2.0).
  - Effective per-slot sample cap = `max(MAX_SAMPLE_BYTES, ceil(w*h*IDR_BPP/8))`,
    so a single IDR fits at 4K (~2 MB) / 8K (~8 MB). Tiny outputs keep the 1 MB
    floor. Used by both layout build and the layout cache key; the builder bitrate
    budget scales with the larger `max_bytes`.
- R2 (failed slot rebuilt every request -> GPU thrash):
  - `_Vmp4SlotBuild` gained `attempts` / `permanent` / `next_retry_at`.
  - Failures whose reason contains `oversize` or `unsupported` are permanent and
    never auto-retried. Transient failures back off
    `min(RETRY_MAX, RETRY_BASE * 2**attempts)`.
  - New `PT_PASSTHROUGH_SEEK_VMP4_SLOT_RETRY_BASE` (5s) /
    `PT_PASSTHROUGH_SEEK_VMP4_SLOT_RETRY_MAX` (60s).
- R3 (matter acquire hang):
  - Build worker now uses
    `acquire_matter(blocking=True, timeout=PT_PASSTHROUGH_SEEK_VMP4_SLOT_MATTER_TIMEOUT)`
    (default 20s). Timeout becomes a transient `slot-real-builder-matter-timeout`
    failure that backs off instead of hanging `building` forever.

Files: `config.py`, `http_app/routes_media.py`, `tests/test_passthrough_vmp4_slot.py`.

Validation:

- `tests/test_passthrough_vmp4_slot.py` -> `17 passed`.
- slot/config/settings/cache/content-directory regression -> `143 passed, 8 subtests passed`.
- `py_compile` of the four modules + test -> passed.

Real-device watch points:

- `payload-oversize` should largely disappear; raise `IDR_BPP` if it persists.
- Failed slots must not rebuild every second; permanent failures build once.
- Under live + seek concurrency, slot builds must not stay `building` forever.
- Still open: one-IDR-per-slot smoothness (needs frame-level sample layout) and
  template `hvcC` vs PyNv parameter-set consistency.

## Skybox/4XVR Transient Slot-Not-Ready Compatibility

Latest Quest logs showed the real PyNv HEVC slot builder was producing payloads,
but players saw transient HTTP failures before the first requested slot was
ready.

Observed log pattern:

- Skybox `Range: bytes=0-` got an init-only `206` of a few KB, then requested the
  next byte range and received `503 slot-not-ready`. Slot0 became ready less than
  a second later, but the player had already failed startup.
- 4XVR sent no `Range` header to the seekable VMP4 URL and received immediate
  `503 slot-not-ready`, then tried live/raw paths and still failed.

Implemented fix:

- Added `PT_PASSTHROUGH_SEEK_VMP4_SLOT_READY_WAIT` (default `8.0s`).
- Ready-only VMP4 requests now schedule the requested slot build and wait briefly
  for the real HEVC payload before responding.
- Skybox-style ranged requests no longer expose slot0 build latency as init-only
  `206` followed by `503`. When a range starts inside an unready slot, the route
  waits for that slot and then returns the real payload. If a later slot is still
  unavailable and the response already has ready bytes before it, the route can
  still bound the open range before the unavailable slot.
- 4XVR-style no-Range requests now use a ready-only streaming iterator: before
  each slot is emitted, it waits for the real slot payload. It does not emit
  placeholder bytes and does not fall back to cache mode. A later timeout logs a
  warning and aborts the stream instead of silently serving placeholders.
- Added response diagnostics:
  `X-Passthrough-VMP4-Slot-Wait`,
  `X-Passthrough-VMP4-Slot-Wait-Ready`,
  `X-Passthrough-VMP4-Slot-Wait-Seconds`.

Files changed:

- `config.py`
- `http_app/routes_media.py`
- `tests/test_passthrough_vmp4_slot.py`
- `summary/summary_20260619_PASSTHROUGH_SEEK_REWORK_PLAN_CN.md`
- `prompt/HANDOVER_20260620.md`

Validation:

- `tests/test_passthrough_vmp4_slot.py` -> `18 passed`.
- slot/config/settings/cache/content-directory regression ->
  `144 passed, 8 subtests passed`.
- `py_compile` for `config.py`, `http_app/routes_media.py`,
  `pipeline/passthrough_vmp4_slot.py`, `pipeline/pynv_stream.py`,
  `tests/test_passthrough_vmp4_slot.py` -> passed.

Next real-device watch points:

- Skybox should no longer show a tiny init-only `206` followed by
  `503 slot-not-ready`.
- Ranged slot requests should show `X-Passthrough-VMP4-Slot-Wait-Ready: 1` when
  they had to wait.
- 4XVR no-Range seekable requests should no longer immediately return 503. If it
  still fails, investigate structural MP4 compatibility next: one-IDR-per-slot
  cadence and PyNv-derived `hvcC` consistency.

## A/B/C: Event-loop Fix, Tight slot_size, Offline Decode Harness

Quest3 (Skybox/4XVR) still failed after the ready-wait slice. Gatekeeping pass
fixed a concurrency bug, reversed a wrong earlier change, and added an offline
decode gate that produced an important diagnosis.

### A - ranged ready-wait was blocking the asyncio event loop
- The slice-14 ready-wait for ranged requests ran inline in `async def
  passthrough_seek_get` as a `time.sleep` poll (up to 8s), freezing every other
  connection and likely causing the very 503/timeout cascades being chased.
- Fix: GET now does `await run_in_threadpool(_seek_vmp4_slot_response, ...)`.
  The no-Range wait already ran inside the streaming generator (threadpool) and
  was fine.

### B - tighten slot_size (revert slice-13 R1)
- Real 8K alpha IDRs are 50-275KB; they never oversize. R1 had enlarged
  slot_size to the ~2.5MB stride cap, putting ~2.2MB of filler NAL INSIDE each
  access unit. Wrong direction for mobile HW decoders.
- `slot_size` = the stsz/access-unit size the decoder reads; `slot_stride -
  slot_size = slot_gap` is unreferenced. Smaller slot_size => smaller AU.
- Fix: removed `PT_PASSTHROUGH_SEEK_VMP4_SLOT_IDR_BPP` and
  `_vmp4_slot_effective_max_sample_bytes`; default
  `PASSTHROUGH_SEEK_VMP4_SLOT_MAX_SAMPLE_BYTES` 1MB -> 512KB. 8K AU filler now
  <=512KB (was ~2.2MB); the bulk lands in the unreferenced gap. R2 retry policy
  unchanged.

### C - offline decode harness `tools/vmp4_slot_decode_check.py`
- Builds the real layout, fills the first N slots with a real HEVC IDR (ffmpeg
  template, or `--payload-file` to inject a real slot_XXXXXX.bin), serves them
  through the production `iter_vmp4_slot_range`, then runs ffprobe + ffmpeg.
- `uv run python -m tools.vmp4_slot_decode_check --source videos/72456_3840p.mp4 --slots 5`

### C key finding (changes the next direction)
- 7680x3840, 512KB cap: ffprobe sees `hevc 7680x3840 nb_frames=190`; `ffmpeg
  -frames:v 5 -f null -` => DECODE OK. The old 2.5MB-AU cap also decodes OK.
- So the virtual MP4 container is structurally VALID; ffmpeg (tolerant software
  decode) cannot reproduce the Quest failure. The harness is a necessary-not-
  sufficient gate (it catches broken containers, not HW per-AU limits).
- Therefore the Quest failure is most likely NOT the container structure. Prime
  suspects now: (a) mobile HW-decoder per-AU input limits on giant filler AUs
  (B cuts AU 2.5MB->512KB to de-risk), (b) the one-IDR-per-slot ~1fps cadence,
  (c) startup HTTP behavior (A + slice 14 improved this). Stop iterating on HTTP
  timing.

Files: `config.py`, `http_app/routes_media.py`, `tools/vmp4_slot_decode_check.py`,
`tests/test_passthrough_vmp4_slot.py`, `tests/test_config_defaults.py`.

Validation: regression `144 passed, 8 subtests`; harness DECODE OK.

Next real-device watch points:
- Confirm `X-Passthrough-VMP4-Slot-Size` is ~512KB now.
- If still failing: container is proven valid, so move to cadence/structure
  (frame-level samples per slot instead of one IDR), or diff against the working
  `/passthrough_live` to capture where Quest actually rejects the seek stream.

## Real-Device Diagnosis + Frame-Level Layout Feasibility Proof

Quest results: SKYBOX shows picture but plays ~1fps (slideshow); 4XVR froze;
MoonVR green frames + no progress bar.

Log diagnosis (debug_output/server.log):
- Pipeline is end-to-end healthy: slot builds succeed (236-286KB IDRs, all under
  the 512KB cap), no-Range now 200, ranged 206 with correct ranges, no server
  errors.
- Root cause of PPT is structural: one slot == one MP4 sample with a ~1.2s stts,
  so the whole title is 0.83 fps. Not a decode/HTTP issue.
- Each slot GPU build is ~0.5-1.7s, so per-request on-demand encode cannot keep
  up with realtime either.
- `1.mp4 mode=two_dvr` permanently fails (`slot-real-builder-unsupported-mode`),
  so that DLNA item returns 503 forever (a likely 4XVR/MoonVR failure source).

Two structural ceilings: (1) MP4 fixed-offset byte-stable layout vs variable
frame sizes; (2) on-demand GPU latency cannot produce realtime fps.

User direction: stay realtime (cache_file rejected), no fMP4, salvage the current
plain MP4.

Validated method - frame-level fixed-budget layout:
- Every frame becomes its own MP4 sample with stts=1/fps -> real fps.
- Each frame gets a FIXED per-frame byte budget; real NAL + filler NAL pad to it,
  so co64/stsz are fixed up front -> still single moov, byte-stable, plain MP4.
- Filler shrinks from ~2.4MB per AU to tens of KB per frame.
- Drop strict virtual_size==source_size (8K realtime bitrate cannot fit the
  source byte budget); declare total from a realtime-bitrate estimate like live.
- Realtime: a background sequential encoder (reuse the live PyNv loop) fills the
  fixed frame slots ahead of the read cursor; seek repositions the decoder.

Offline proof (GPU-free): `tools/vmp4_frame_filler_proof.py`
- libx265 (bframes=0, matching HEVC_BF=0) 60-frame GOP, repacked as per-frame
  fixed-budget + filler in a plain ftyp+moov+mdat.
- ffprobe: hevc 1920x960 nb_frames=60 avg_frame_rate=30/1 duration=2.0
- ffmpeg decode: OK. RESULT: decode=OK real_fps=OK -> frame-level layout viable.
- Note: libx265 default (B-frames) triggers a non-monotonic DTS muxer warning
  (no ctts emitted); the real encoder is HEVC_BF=0, so bframes=0 is clean.

Next (backend to build, not yet implemented):
1. Frame-level layout builder: slot_count*K frame samples, per-frame budget,
   fixed co64/stsz, stts=1/fps, stss=GOP heads, total from realtime estimate.
2. Background sequential realtime encoder feeding fixed offsets ahead of cursor;
   seek = decoder reposition.
3. two_dvr: implement it or stop exposing its seek item (currently perma-503).

## slot_frames Backend: Frame-Level Real-FPS Layout + Background GOP Encode

Implements the salvage method end to end (user chose: do it complete). New
backend value `PT_PASSTHROUGH_SEEK_VMP4_BACKEND=slot_frames` (now the default),
beside the unchanged `slot` (1fps) and `cache_file`.

What it does:
- Every frame is its own MP4 sample, stts=1/fps -> real fps; stss marks only GOP
  heads (so seeking lands on real IDRs, not P-frames -> avoids MoonVR green).
- Each frame has a fixed byte budget (IDR vs P); real HEVC bytes + filler pad to
  it so co64/stsz are fixed up front -> still plain ftyp+moov+mdat, single moov,
  byte-stable.
- Drops virtual_size==source_size; declares total from the per-frame budgets.
- Patches mdhd timescale+duration so 1/fps ticks map to real fps (validated:
  ffprobe 30/1, ffmpeg decode OK).

Background realtime encode:
- Reuses build_passthrough_hevc_annexb_gop (one PyNv encode per GOP).
- New split_hevc_annexb_access_units() cuts the GOP Annex-B into per-frame AUs
  (one VCL NAL == one frame at HEVC_BF=0); each -> length-prefixed sample ->
  frame_XXXXXX.bin (atomic). Over-budget frame fails that GOP (permanent).
- The body is a per-GOP waiting generator that builds the current GOP and
  prefetches ahead (PT_PASSTHROUGH_SEEK_VMP4_FRAMES_PREFETCH=3) as the player
  pulls bytes; runs in the StreamingResponse threadpool. Start GOP not ready in
  time -> 503 + Retry-After.
- two_dvr -> immediate 409 frames-mode-unsupported (no perma-503).

New config: PT_PASSTHROUGH_SEEK_VMP4_FRAMES_IDR_BYTES (768K),
_P_BYTES (160K), _PREFETCH (3). GUI default + migration
20260620_seek_vmp4_frames_default (slot -> slot_frames).

Files: pipeline/passthrough_vmp4_frames.py (new), passthrough_vmp4_slot.py
(splitter), http_app/routes_media.py, config.py, ui/settings.py,
tests/test_passthrough_vmp4_frames.py, tests/test_config_defaults.py,
tests/test_settings.py, tools/vmp4_frame_filler_proof.py.

Validation: 150 passed, 8 subtests; offline frames layout decodes at real fps.

Real-device watch / tuning (GPU path is the only unvalidated part):
- Watch for `frames-oversize` in logs; bump PT_PASSTHROUGH_SEEK_VMP4_FRAMES_P_BYTES
  if 8K P-frames exceed 160K.
- ~1s GOP encode vs 1.2s content: prefetch must stay ahead; raise prefetch if it
  rebuffers.
- Headers to confirm: X-Passthrough-VMP4-Backend: slot_frames,
  X-Passthrough-VMP4-Frames-Fps, X-Passthrough-VMP4-Frames-Wait-Ready.
- DLNA DIDL size (source) vs HTTP total (budget estimate) differ; confirm players
  rely on HTTP Content-Range. Consider aligning DIDL size later.
