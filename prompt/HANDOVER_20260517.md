# HANDOVER 2026-05-17

## UI decoder/preset rollback

- Reason: `ThreadedDecoder` brought alpha passthrough flicker/stability issues and did not show enough steady-state benefit beyond startup speed.
- Default/UI path is now back to simple decode:
  - `ui.settings.Settings.server_env()` always exports `PT_PASSTHROUGH_PYNV_DECODER=simple`.
  - Threaded batch/buffer env is pinned to `1/2` for compatibility, but the UI no longer exposes threaded profiles.
- Removed the three UI-facing "VRAM usage / 显存占用" controls:
  - realtime server performance panel
  - offline single conversion
  - offline batch conversion
- Replaced them with "Quality / Speed / 画质速度":
  - `ultrafast` -> `PT_PASSTHROUGH_PYNV_PRESET=P1`
  - `medium` -> `PT_PASSTHROUGH_PYNV_PRESET=P4`
  - default is `ultrafast` / P1.
- Offline conversion no longer defaults to P5/P7:
  - `offline/convert.py` default preset uses `config.PASSTHROUGH_PYNV_PRESET`.
  - `tools/offline_passthrough.py` and `tools/offline_alpha_passthrough.py` direct CLI defaults also use `config.PASSTHROUGH_PYNV_PRESET`.

## Validation

- `py_compile` passed for:
  - `ui/settings.py`
  - `ui/pages/home_page.py`
  - `ui/pages/offline_page.py`
  - `offline/convert.py`
  - `tools/offline_passthrough.py`
  - `tools/offline_alpha_passthrough.py`
- Translation JSON parse passed for `en_US`, `zh_CN`, `ja_JP`.
- Default `Settings().server_env()` verified as:
  - `PT_PASSTHROUGH_PYNV_PRESET=P1`
  - `PT_PASSTHROUGH_PYNV_DECODER=simple`
  - threaded batch/buffer `1/2`
- `PT_PASSTHROUGH_PYNV_PRESET=P4` env override verified to flow into `offline.convert.RVM_DEFAULT_ARGS["preset"]`.

## Quality/speed split for realtime vs offline

- Realtime/home page keeps `quality_speed` with default `ultrafast` -> P1.
- Offline conversion now uses separate `offline_quality_speed` with default `medium` -> P4.
- Offline single and batch quality/speed combos expose:
  - `ultrafast` -> P1
  - `medium` -> P4
  - `veryslow` -> P7
- Offline process startup overrides `PT_PASSTHROUGH_PYNV_PRESET` from `offline_quality_speed`, so offline P4/P7 does not change realtime server preset.
- Compatibility: if an older `ui_settings.json` only has the previous shared `quality_speed`, it is migrated into `offline_quality_speed`, and realtime `quality_speed` is reset to the P1 default.
- Validation:
  - `py_compile` passed for UI settings/pages.
  - Translation JSON parse passed.
  - Local settings load verified as realtime P1 and offline P4.

## Realtime RVM model default

- Realtime `config.MODEL_PATH` default changed back to `models/rvm_mobilenetv3_fp32.onnx`.
- FP16 remains available only through explicit `PT_MODEL_PATH` / `MODEL_PATH` override for benchmarking.
- Offline RVM defaults were already FP32 and were left unchanged.
- Validation:
  - `py_compile config.py` passed.
  - Import check prints `rvm_mobilenetv3_fp32.onnx`.

## Short DLNA live item green/alpha mode fix

- Bug: for videos shorter than the live chapter threshold, DLNA exposes live passthrough as direct virtual files instead of virtual chapter folders. When green and alpha were both enabled, some clients could treat both short virtual files as green.
- Root cause: short virtual file ids (`lg_...` / `la_...`) were not recognized by `_id_to_live()` / `BrowseMetadata`; only long-video chapter container ids (`pl_...` / `pla_...`) were mode-aware. A client that re-browsed metadata by item id could lose `mode=alpha`.
- Fix:
  - Added explicit short live item prefixes `lg_` and `la_`.
  - `_id_to_live()` now recognizes both short item ids and long chapter container ids.
  - Short live items now carry `passthrough_mode`.
  - `BrowseMetadata` returns item metadata for short live items, preserving `?mode=green` / `?mode=alpha`.
- Validation:
  - Added tests for distinct short live ids/URLs and alpha metadata.
  - `python -m unittest tests.test_content_directory_modes` passed.
  - `py_compile` passed for `dlna/content_directory.py` and the updated test.
