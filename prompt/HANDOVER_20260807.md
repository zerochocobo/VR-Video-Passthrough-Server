# Handover 2026-08-07

## Face Beauty Settings Entry Gate

- Added persisted `face_beauty_card_visible` setting, defaulting to hidden like mosaic restoration.
- Added a Settings-page debug switch with Chinese, English, and Japanese labels.
- Connected the switch to both the Dashboard live card and Tools offline entry.
- Hiding the entry also disables `face_beauty_enabled`; startup synchronization enforces the same invariant.
- Fixed Dashboard 2D grid rebuilding so hidden Face Beauty cards are filtered before row/column placement and later cards close the gap.
- `open_face_beauty` now rejects access when the entry gate is disabled.

## Verification

- `python -m py_compile ui/settings.py ui/pages/settings_page.py ui/pages/dashboard_page.py ui/pages/tools_page.py ui/main_window.py`
- `pytest tests/test_settings.py tests/test_ui_smoke.py -q`: 30 passed, 14 subtests passed.
- Translation JSON files parse successfully with UTF-8.
