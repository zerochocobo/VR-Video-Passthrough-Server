# HANDOVER 2026-06-18

## SI Progressive M1 Default-On

### Change

- Added SI-specific progressive transport switches in `config.py`:
  - `PT_SI_PROGRESSIVE_ENABLED`, default `1`.
  - `PT_SI_PROGRESSIVE_DLNA`, default `1`.
- `/media_si` now uses `SI_PROGRESSIVE_ENABLED` instead of the legacy
  `PASSTHROUGH_SEEK_ENABLED` gate. This keeps M1 SI testing on by default
  without turning on the old `/passthrough_seek` experiment.
- DLNA `[SI]` Browse visibility now uses
  `SI_PROGRESSIVE_ENABLED && SI_PROGRESSIVE_DLNA`. With default settings,
  MP4 files that have same-stem `.si.wav` sidecars should expose `[SI]` items
  immediately.
- Legacy seekable passthrough defaults remain unchanged:
  `PASSTHROUGH_SEEK_ENABLED=0`, `PASSTHROUGH_SEEK_DLNA=0`.

### Verification

- `.\.venv\Scripts\python.exe -m pytest tests\test_config_defaults.py tests\test_si_mix.py tests\test_si_virtual_mp4.py tests\test_routes_media_cache.py -q` -> 72 passed.
- `.\.venv\Scripts\python.exe -m pytest tests\test_content_directory_modes.py -q -k "not versioned_live_id_resolves"` -> 34 passed, 1 deselected, 4 subtests passed.
- `PYTHONPYCACHEPREFIX=debug_output\pycache_compile .\.venv\Scripts\python.exe -m py_compile config.py pipeline\si_virtual_mp4.py http_app\routes_media.py dlna\content_directory.py tests\test_si_mix.py tests\test_config_defaults.py` -> passed.
- After runtime cache permissions were fixed, `_ensure_cache_dir()` now returns
  `runtime_cache\si_virtual_mp4`; the 30s progressive virtual MP4 smoke wrote
  the AAC sidecar there and `ffprobe` still recognized
  `hevc,video,8192,4096,30.046000 | aac,audio,30.016000`.

### Notes For Testing

- Restart the server so `config.py` defaults reload.
- `[SI]` entries should appear in DLNA when SI mixing is enabled and a matching
  `.si.wav` exists.
- To temporarily hide only this M1 SI route, set:
  `PT_SI_PROGRESSIVE_ENABLED=0` or `PT_SI_PROGRESSIVE_DLNA=0`.

## SI Progressive First-Play Timeout Diagnosis

### Finding

- SKYBOX first playback of `SI_TEST_8K.mp4` hit `/media_si` with
  `Range: bytes=0-`, but the request waited for full M1 preparation before
  response headers:
  - `00:29:29` started building mixed AAC sidecar.
  - `00:32:14` finished virtual progressive layout.
  - Request history recorded `elapsed_ms=164719.989` before `206`.
- That is long enough for SKYBOX to show a spinner and fail. This is not yet a
  post-response MP4 compatibility failure; it is a first-play cold-prepare
  timeout.
- The generated layout was:
  `size=7449706438`, `moov=6173439`, samples `180240+140959`, regions `321200`.

### Fix

- `pipeline/si_virtual_mp4.py` now uses a per-layout single-flight build lock,
  so concurrent Range requests/background jobs do not duplicate the 8GB scan.
- Added `prewarm_progressive_si_virtual_mp4(...)`, a daemon-thread background
  prewarmer guarded by a single prewarm semaphore.
- `dlna/content_directory.py` queues SI progressive prewarm when `[SI]` items are
  generated during Browse. This lets the expensive sidecar/layout build happen
  before the user presses play, instead of inside SKYBOX's first media request.

### Verification

- `.\.venv\Scripts\python.exe -m pytest tests\test_si_mix.py tests\test_si_virtual_mp4.py tests\test_config_defaults.py tests\test_routes_media_cache.py -q` -> 72 passed.
- `.\.venv\Scripts\python.exe -m pytest tests\test_content_directory_modes.py -q -k "not versioned_live_id_resolves"` -> 34 passed, 1 deselected, 4 subtests passed.
- `PYTHONPYCACHEPREFIX=debug_output\pycache_compile .\.venv\Scripts\python.exe -m py_compile config.py pipeline\si_virtual_mp4.py http_app\routes_media.py dlna\content_directory.py tests\test_si_mix.py tests\test_config_defaults.py` -> passed.

### Testing Note

- The running server process still needs restart to pick up this prewarm fix.
- In the already-running old process, `SI_TEST_8K.mp4` layout has been built in
  memory after the first failed attempt, so retrying the same item may start
  immediately until the process restarts.

## SI Progressive Audio Sync Diagnosis

### Finding

- After the prewarm fix, `/media_si` playback succeeded, but the user reported
  obvious audio/video desync.
- The successful runtime log used the cached sidecar:
  `runtime_cache\si_virtual_mp4\37be0fbab73c053fa0708d1ffc8a04afaf05931006c4894421482301c859ec62.audio.mp4`.
  That file was generated at `00:30:53`, before the SI delay default/local
  setting was changed.
- `utils/si_filter.py` previously defaulted `DEFAULT_SI_DELAY_SECONDS = 1.0`,
  and `runtime_cache\ui_settings.json` also had `"si_delay_seconds": 1.0`.
  This deliberately injects `adelay=1000` into the SI WAV path before mixing,
  so it is a direct explanation if the observed desync is the SI track being
  about one second late.
- The progressive virtual MP4 cache key includes the full `SIMixParams`
  payload, including `si_delay_seconds`, so changing the delay to `0.0` will
  generate a different AAC sidecar instead of reusing the old delayed one.

### Fix

- Changed `DEFAULT_SI_DELAY_SECONDS` from `1.0` to `0.0` in
  `utils/si_filter.py`.
- Updated local `runtime_cache\ui_settings.json` to `"si_delay_seconds": 0.0`
  for the next manual test.
- Updated the clamp test expectation in `tests\test_si_mix.py` so invalid delay
  values fall back to the new default `0.0`.

### Verification

- `.\.venv\Scripts\python.exe -m pytest tests\test_si_mix.py tests\test_si_virtual_mp4.py tests\test_config_defaults.py tests\test_routes_media_cache.py -q` -> 72 passed.
- `.\.venv\Scripts\python.exe -m pytest tests\test_content_directory_modes.py -q -k "not versioned_live_id_resolves"` -> 34 passed, 1 deselected, 4 subtests passed.
- `git diff --check` -> passed; only existing Git CRLF warnings were reported.
- Prebuilt the `0.0s` delay sidecar for `videos\SI_TEST_8K.mp4`:
  `runtime_cache\si_virtual_mp4\b6a4e5fc94f1f98a15f57e2abeddbd39a12f1711a82dbb42bd04ac9debd7b0ab.audio.mp4`
  (`72,568,392` bytes).
- `ffprobe` on the new sidecar reports `aac`, `start_time=0.000000`,
  `duration=3007.104000`, `nb_frames=140959`.
- Building the progressive virtual layout with `SIMixParams(si_delay_seconds=0.0)`
  succeeded using the new sidecar:
  `size=7449678180`, `moov=6173439`, samples `180240+140959`,
  regions `321200`.

### Next Test Note

- Restart the server before retesting so the process reloads the new default and
  UI setting.
- On the next Browse/prewarm, the new `0.0` delay setting should build a new
  `runtime_cache\si_virtual_mp4\*.audio.mp4` sidecar. For `SI_TEST_8K.mp4`, that
  sidecar is already prebuilt as `b6a4e5...audio.mp4`. The old `37be0f...`
  sidecar may remain on disk but should not be selected for `si_delay_seconds=0.0`.
- If playback still desyncs after the 0.0-delay sidecar is used, capture whether
  the audio is early or late and whether the offset is fixed or drifts. Then
  inspect MP4 edit-list/AAC priming behavior in the generated sidecar and the
  copied virtual audio track.

### Follow-up Notes

- `si_delay_seconds` semantics clarified: it is the relative offset applied only
  to SI WAV (`[1:a:0]`) before mixing. Positive values delay SI relative to the
  source audio/video; `0.0` means no artificial SI delay.
- Added sidecar build/reuse observability in `pipeline/si_virtual_mp4.py`.
  Future logs include `digest`, `delay`, mix channel, source/SI volumes, and
  ducking flag, so real-device tests can prove whether the server selected the
  old `1.0s` sidecar or the new `0.0s` sidecar.
- Wrote external-expert research summaries:
  - `summary\summary_20260618_SI_PROGRESSIVE_AV_SYNC_RESEARCH_CN.md`
  - `summary\summary_20260618_SI_PROGRESSIVE_AV_SYNC_RESEARCH_EN.md`
- Re-ran verification after the logging patch:
  - `.\.venv\Scripts\python.exe -m pytest tests\test_si_mix.py tests\test_si_virtual_mp4.py tests\test_config_defaults.py tests\test_routes_media_cache.py -q` -> 72 passed.
  - `.\.venv\Scripts\python.exe -m pytest tests\test_content_directory_modes.py -q -k "not versioned_live_id_resolves"` -> 34 passed, 1 deselected, 4 subtests passed.
  - `git diff --check` -> passed; only existing Git CRLF warnings were reported.

### Correction After User Clarification

- User clarified the actual perceived problem was original source audio vs
  picture desync, not SI translation voice vs original-audio offset.
- This invalidates the earlier "likely caused by `DEFAULT_SI_DELAY_SECONDS=1.0`"
  conclusion for the reported symptom. `si_delay_seconds` only applies to SI WAV
  (`[1:a:0]`) and cannot explain the original source audio moving against the
  picture.
- Replaced both expert summaries with corrected versions. The corrected
  conclusion is:
  - `DEFAULT_SI_DELAY_SECONDS=1.0 -> 0.0` is still a good default for SI voice
    alignment, but it is not a root-cause closure for original-audio A/V desync.
  - Sidecar/filter timing now has direct evidence against being the culprit.
- Added PCM cross-correlation evidence comparing source audio against both
  sidecars. Positive lag means sidecar audio is later than source audio:
  - `37be...` (`delay=1.0`): about `4.96ms` at start, `4.48ms` at 600s and 1500s.
  - `b6a4...` (`delay=0.0`): about `4.98ms` at start, `4.48ms` at 600s and 1500s.
- Interpretation: original-audio content inside both sidecars is aligned with
  source audio within about 5ms and does not drift. `si_delay_seconds` affects
  only the SI voice component.
- Remaining investigation should focus on actual HTTP virtual MP4 output and
  player interpretation:
  - Probe/decode `/media_si` itself, not only the sidecar.
  - Compare decoded `/media_si` audio against source audio.
  - Dump the actual rewritten virtual `moov` if needed.
  - Run a control test: same player plays original `SI_TEST_8K.mp4` directly or
    through non-SI DLNA item.

## SI Progressive Audio Edit-List A/B Experiment

### Why

- Expert follow-up reframed the likely cause: not `si_delay`, not sidecar PCM
  drift, but a player compatibility issue around asymmetric handling of:
  source video `edts`/negative DTS/`ctts` versus imported AAC sidecar audio
  priming `edts/elst`.
- To test that without touching video timing or sample payload bytes, add a
  narrow switch that can remove only the imported audio track edit list from
  the virtual moov.

### Change

- `config.py`
  - Added `SI_AUDIO_EDIT_MODE` from `PT_SI_AUDIO_EDIT_MODE`.
  - Supported values: `remove`, `preserve`.
  - Default is now `remove` for M1 device testing.
- `pipeline/si_virtual_mp4.py`
  - `_rewrite_trak(..., drop_edts=True)` skips `edts` when requested.
  - `_build_moov(..., audio_edit_mode=...)` applies this only to the imported
    audio trak. Source video trak is untouched.
  - Layout cache digest / ETag now include `audio_edit_mode`; sidecar file cache
    remains keyed only by media/stat/SI mix params.
  - `ProgressiveSIVirtualMp4.audio_edit_mode` records the active mode.
- `http_app/routes_media.py`
  - Adds `X-SI-Audio-Edit: remove|preserve` to `/media_si` responses.
- Tests now cover both preserve/remove moov behavior and the new response
  header.

### Verification

- Real `SI_TEST_8K.mp4` default layout after the change:
  - `audio=b6a4e5fc94f1f98a15f57e2abeddbd39a12f1711a82dbb42bd04ac9debd7b0ab.audio.mp4`
  - `audio_edit=remove`
  - `size=7449678144`
  - `moov=6173403`
  - samples `180240+140959`
  - regions `321200`
  - `etag=be9eb7fa1da278feee64a2ee1ec2aa00`
- Actual init segment inspection:
  - `video_edts=True`
  - `audio_edts=False`
  - confirms only imported audio `edts` was removed.
- `.\.venv\Scripts\python.exe -m pytest tests\test_si_mix.py tests\test_si_virtual_mp4.py tests\test_config_defaults.py tests\test_routes_media_cache.py -q` -> 74 passed.
- `.\.venv\Scripts\python.exe -m pytest tests\test_content_directory_modes.py -q -k "not versioned_live_id_resolves"` -> 34 passed, 1 deselected, 4 subtests passed.
- `git diff --check` -> passed; only existing Git CRLF warnings were reported.

### Testing Note

- Default next user test is the `remove` experiment. Restart server and
  re-Browse the `[SI]` item before playback.
- Confirm in response/log:
  - `X-SI-Audio-Edit: remove`
  - `built SI progressive virtual MP4 ... audio_edit=remove ...`
- To A/B old behavior:
  - set `PT_SI_AUDIO_EDIT_MODE=preserve`
  - restart server
  - re-Browse the `[SI]` item
  - test again.

## SI Progressive Cache Stall Fix

### Finding

- User confirmed the cold first-play stall and folder Browse freeze came from
  the SI progressive cache path:
  - `_si_item_from_video()` queued prewarm for every `[SI]` item during Browse.
  - The old prewarmer spawned one daemon thread per item and serialized them
    behind `Semaphore(1)`, so a folder with many SI videos created many blocked
    threads and an unbounded amount of pending cold work.
  - Foreground `/media_si` layout builds did not share that prewarm semaphore,
    so playback could still run a second heavy ffmpeg/layout build beside a
    background prewarm.
  - `read_media_sample_table(video, "video")` used PyAV demux, causing a full
    source-file packet scan even though the needed sample table data already
    lives in `moov`.

### Change

- `pipeline/si_virtual_mp4.py`
  - Added pure `moov` parsing for video sample tables:
    - `stco/co64` + `stsc` + `stsz` expand sample offsets and sizes.
    - `stts` + `ctts` compute DTS/PTS.
    - `stss` marks keyframes.
  - `read_media_sample_table(..., "video")` now uses the `moov` parser first and
    falls back to PyAV only when parsing fails. Audio sidecar sample tables still
    use PyAV because the sidecar is small.
  - Replaced one-thread-per-prewarm with a single background worker and bounded
    priority queue.
  - Added a global cold-build slot shared by background prewarm and foreground
    playback. Background prewarm runs as low priority and can be cancelled when
    a foreground playback build arrives.
  - `build_mixed_audio_sidecar()` now uses cancellable `Popen` instead of
    blocking `subprocess.run()`. Low-priority ffmpeg processes use
    `BELOW_NORMAL_PRIORITY_CLASS` on Windows when available.
- `config.py`
  - Added `SI_BROWSE_PREWARM_LIMIT`, default `1`.
  - Added `SI_PREWARM_QUEUE_MAX`, default `2`.
- `dlna/content_directory.py`
  - Browse still exposes all `[SI]` items, but only the first
    `SI_BROWSE_PREWARM_LIMIT` SI items in a directory listing attempt background
    prewarm.
  - Later SI items are visible but do not enqueue prewarm work.
- Tests cover:
  - synthetic MP4 `moov` sample-table expansion without media demux.
  - bounded/deduped prewarm queue.
  - DLNA SI item generation with prewarm skipped.
  - new config defaults.

### Verification

- `.\.venv\Scripts\python.exe -m pytest tests\test_si_virtual_mp4.py tests\test_si_mix.py tests\test_config_defaults.py tests\test_routes_media_cache.py -q` -> 77 passed.
- `.\.venv\Scripts\python.exe -m pytest tests\test_content_directory_modes.py -q -k "not versioned_live_id_resolves"` -> 34 passed, 1 deselected, 4 subtests passed.
- `PYTHONPYCACHEPREFIX=debug_output\pycache_compile .\.venv\Scripts\python.exe -m py_compile config.py pipeline\si_virtual_mp4.py dlna\content_directory.py http_app\routes_media.py tests\test_si_virtual_mp4.py tests\test_si_mix.py tests\test_config_defaults.py` -> passed.
- `git diff --check` -> passed; only existing Git CRLF warnings were reported.
- Real source sample-table smoke:
  - `videos\SI_TEST_8K.mp4`
  - parsed `180240` video samples from `moov`
  - `codec=hevc`, `time_base=1/16000`, `duration=3007.003`
  - elapsed `0.765s`
  - first sample `offset=44,size=7486,dts=0,pts=528`
  - last sample `offset=7467723979,size=626,dts=48111792,pts=48112064`
- Real layout smoke with existing cached sidecar:
  - `size=7449678144`
  - `moov=6173403`
  - samples `180240+140959`
  - regions `321200`
  - `audio_edit=remove`
  - elapsed `4.922s`

### Remaining Work

- This implements the high-priority fixes from the user's proposed order:
  - 1: video sample table from `moov`, avoiding the second full 8GB scan.
  - 3: bounded single-worker prewarm queue with foreground priority.
- It does not yet implement step 2: decoupling source-audio extraction from mix
  parameter changes. Changing SI volume/delay/duck still creates a new mixed AAC
  sidecar, though it no longer pays the video PyAV sample-table scan.

### User Review

- User reported `111 passed` across their relevant test set and approved the
  change for merge.
- Real 8GB file cross-check against PyAV demux:
  - all `180240` video samples matched PyAV exactly for `source_offset` and
    `size`.
  - `dts/pts` differed by a constant `528` video ticks, matching the source
    video edit-list `media_time` (`528 @ 16000 ~= 33ms`).
  - This is acceptable because layout playback timing is still carried by the
    copied `stts/ctts/elst` boxes; parsed sample times are only used for virtual
    `mdat` interleave ordering.
- Concurrency review found no deadlock:
  - lock order remains per-digest lock then global build slot.
  - foreground playback cancels lower-priority prewarm and then takes the slot.
  - same-digest playback waits for the prewarm build and then hits the cache.
  - priority queue tuple uses `(priority, sequence, task)`, so `_PrewarmTask`
    objects are never compared.
- Integration note:
  - `hidden_subprocess_kwargs()` only supplies `creationflags/startupinfo`, so it
    does not conflict with the new explicit `stdin/stdout/stderr` Popen args.
- Non-blocking caveats:
  - The remaining first-play cost is still `build_mixed_audio_sidecar` reading
    the large source through ffmpeg and encoding the mixed AAC sidecar.
  - `_run_ffmpeg_sidecar()` polls `communicate(timeout=0.25)` and is fine with
    `-v error`; avoid raising ffmpeg log volume without revisiting PIPE
    draining/backpressure.

## SI Source Audio Cache Decoupling

### Goal

- Implement the user's step 2: decouple source-audio extraction from SI mix
  parameters and minimize first-play blocking time.
- Avoid `ffmpeg -i 8GB.mp4` for every mix-parameter change.

### Change

- `pipeline/si_virtual_mp4.py`
  - Added `build_source_audio_sidecar(video)`:
    - keyed only by source video stat and track index.
    - parses source audio sample table from `moov`.
    - writes a compact `*.source-audio.mp4` containing only source audio samples.
    - preserves the source audio trak timing/description/edit boxes and rewrites
      only `stsc/co64` for the compact cache file.
  - `read_media_sample_table()` now uses the `moov` parser for audio too, with
    PyAV fallback only if parsing fails.
  - `build_mixed_audio_sidecar()` now uses the source-audio sidecar as ffmpeg
    input when available.
  - First cold mix path is optimized further:
    - when source-audio sidecar is missing, ffmpeg receives `pipe:0`;
    - Python generates the audio-only MP4 once and tees it to both ffmpeg stdin
      and the `*.source-audio.mp4` cache;
    - source-audio extraction and AAC mixing therefore overlap instead of
      running serially.
  - If the source-audio cache/pipe path fails, the code logs a warning and falls
    back to the previous full source-video ffmpeg input path for compatibility.
- `tests/test_si_virtual_mp4.py`
  - covers audio-only source sidecar construction and payload/sample table.
  - covers mixed sidecar using an existing cached source-audio input.
  - covers first-cold mixed sidecar using `pipe:0` and creating the source-audio
    cache.

### Verification

- `.\.venv\Scripts\python.exe -m pytest tests\test_si_virtual_mp4.py tests\test_si_mix.py tests\test_config_defaults.py tests\test_routes_media_cache.py -q` -> 80 passed.
- `.\.venv\Scripts\python.exe -m pytest tests\test_content_directory_modes.py -q -k "not versioned_live_id_resolves"` -> 34 passed, 1 deselected, 4 subtests passed.
- `PYTHONPYCACHEPREFIX=debug_output\pycache_compile .\.venv\Scripts\python.exe -m py_compile config.py pipeline\si_virtual_mp4.py dlna\content_directory.py http_app\routes_media.py tests\test_si_virtual_mp4.py tests\test_si_mix.py tests\test_config_defaults.py` -> passed.
- `git diff --check` -> passed; only existing Git CRLF warnings were reported.

### Real 8GB Smoke

- Standalone source-audio cache build:
  - output:
    `runtime_cache\si_virtual_mp4\f954d753e209fcb45a16b2f39f3d2ad177f6e0d1de9265cee895d3d63f9e11df.source-audio.mp4`
  - size `97,919,571` bytes.
  - samples `140958`, `codec=aac`, `duration=3007.104`.
  - elapsed `80.875s`.
- ffmpeg audio stream-copy extraction was also tested as a comparison:
  - `ffmpeg -i videos\SI_TEST_8K.mp4 -map 0:a:0 -vn -c:a copy ...`
  - elapsed `81.282s`.
  - Interpretation: this file's source audio is distributed as sparse ranges;
    the 80s cost is sparse source reads, not Python overhead.
- Mixed AAC from existing source-audio cache:
  - params: original volume `90`, SI volume `90`, delay `0.3`, duck on.
  - output:
    `runtime_cache\si_virtual_mp4\649c3a3f5639ac78c038790780a8b5fdb3e162ec2fd7a6454d1c45bc9bc9dc23.audio.mp4`
  - size `72,537,891` bytes.
  - elapsed `71.438s`.
- First-cold overlapped pipe path:
  - source-audio cache was deleted before the run.
  - params: original volume `80`, SI volume `90`, delay `0.5`, duck off.
  - source-audio cache was recreated and mixed AAC was generated in the same
    run.
  - output:
    `runtime_cache\si_virtual_mp4\a30814a4a65d6df5460907667c22e7aad6d53bde44c65ee8c6d4185e877d35c9.audio.mp4`
  - elapsed `70.203s`.
  - This confirms extraction and mixing overlapped; first-cold time is near the
    slower single stage instead of `80s + 71s`.
- Full virtual layout using the new mixed sidecar:
  - `size=7449683858`
  - `moov=6173435`
  - samples `180240+140959`
  - regions `321200`
  - `audio_edit=remove`
  - elapsed `4.328s`.

### Notes

- Step 2 is now implemented.
- For a completely new large file, first-play still needs full-duration AAC
  mixing before M1 progressive MP4 can return headers, but it no longer performs
  an 8GB ffmpeg source scan and a separate serial source-audio extraction pass.
- After the first source-audio cache exists, changing SI volume/delay/duck only
  rebuilds the mixed AAC sidecar from the small source-audio MP4 plus `.si.wav`.

## SI Sidecar Build Acceleration Plan Follow-up

### Goal

- Implement `summary/summary_20260618_SI_SIDECAR_BUILD_ACCELERATION_PLAN_CN.md`.
- Shorten first `/media_si` playback preparation while preserving audio timing
  and avoiding segmented AAC seam artifacts.

### Change

- `pipeline/si_virtual_mp4.py`
  - Added sequential source-audio extraction:
    - `_copy_file_ranges_sequential_to_outputs()` scans the source file forward
      with a large buffer instead of issuing one cold seek per audio sample.
    - Non-monotonic sample offsets now fall back to the old run-copy path so
      decode order is not changed for unusual files.
  - Added segmented native-AAC implementation:
    - `_plan_mix_segments()`, `_encode_mix_segment()`,
      `_stitch_aac_segments()`, and audio `stts/stsz/co64/edts` rewrite helpers.
    - Stitching preserves the first segment priming when used and normalizes
      segment-local boundary `stts` trim artifacts.
  - Added encoder/implementation cache variant to `_cache_digest()` so sidecars
    are not reused across encoder or segmented/single implementation changes.
  - Default mix path now selects continuous `aac_mf` on Windows when available;
    native segmented AAC remains behind explicit `PT_SI_MIX_SEGMENTED_AAC=1`.
- `config.py`
  - Added `SI_AUDIO_EXTRACT_MODE=sequential`.
  - Added `SI_MIX_PARALLEL_MAX`, `SI_MIX_SEGMENT_WARMUP_MS`.
  - Added `SI_MIX_ENCODER=auto` and `SI_MIX_SEGMENTED_AAC=0`.
- `tests/test_si_virtual_mp4.py`
  - Added coverage for segment planning, sequential extraction, and AAC segment
    stitching with preserved first priming.
- `tests/test_config_defaults.py`
  - Added defaults for the new SI acceleration switches.

### R1 Decision

- Default final behavior is continuous `aac_mf` where available:
  - no segmented AAC seam;
  - no sidecar `edts` priming (`media_time=0`);
  - real output on `SI_TEST_8K.mp4`: `140958` audio samples,
    `sum_dur=144341016`, `edts=0`, `priming=0`.
  - Reviewer independently measured `0 sample / 0 ms` lag vs a lossless mix
    reference, so removing imported audio `elst` in the virtual moov does not
    expose an AAC priming delay for the default `aac_mf` path.
- Native segmented AAC was implemented and tested, but it is not the default:
  - after preserving first priming and matching old native `stts/elst`
    (`media_time=1024`), decoded PCM still diverged from continuous native AAC
    after the first seam:
    - full decode compare: `max_abs=0.3326`, `rms=0.01033`.
    - increasing warmup to `10s` still left seam-adjacent windows around
      `0.02-0.03` RMS.
  - Conclusion: independent native AAC segment encodes are not
    sample-identical to one continuous native AAC encode on this file, so the
    segmented path is kept as an explicit experiment rather than shipped as the
    safe default.

### Real 8GB Measurements

Measured on `videos/SI_TEST_8K.mp4`; application cache was deleted where noted,
but OS page cache could not be forced cold in this environment.

- Standalone sequential source-audio extraction:
  - output size `97,919,571` bytes.
  - samples `140958`.
  - elapsed `4.594s`.
  - ffmpeg full decode returned `0`.
- Default continuous `aac_mf` remix from existing source-audio cache:
  - params: original `90`, SI `80`, delay `0.0`, duck off.
  - output:
    `runtime_cache\si_virtual_mp4\3237555fe207b7087d3e0d97c0d8fbf99cd003df51df59549f55c743f17e70b3.audio.mp4`
  - size `73,815,412` bytes.
  - elapsed `23.718s`.
- Full first-play layout with source-audio and mixed sidecar cache deleted:
  - source-audio cache and mixed AAC sidecar were recreated in one pipe-overlap
    run.
  - elapsed `29.703s`.
  - samples `180240+140958`, regions `321199`, moov `6173412`.
- Native segmented AAC benchmark:
  - 8 segments, `1000ms` warmup, existing source-audio cache.
  - elapsed about `18.8s`.
  - Not default because of the R1 PCM failure above.

### Quality / Platform Notes

- `aac_mf` is a speed/quality tradeoff at the same bitrate:
  - reviewer measured SNR around `34.5 dB` at `192k` vs native AAC around
    `40.4 dB`.
  - A local attempt to pass `-b:a 256k` to `aac_mf` still produced an ffprobe
    stream bit rate around `194871`, and `ffmpeg -h encoder=aac_mf` exposes no
    encoder-specific quality/bitrate options in this build.
  - The code therefore keeps the historical `192k` command and documents the
    tradeoff instead of promising an ineffective bitrate increase.
  - Use `PT_SI_MIX_ENCODER=aac` when audio quality is more important than
    first-play latency.
- The current `~30s` first-play target is Windows-only:
  - it depends on ffmpeg exposing `aac_mf`.
  - non-Windows hosts fall back to continuous native `aac`, which preserves the
    safe no-seam behavior but can return to roughly the old `~70s` encode cost.
  - A future Linux/container acceleration path should evaluate an available fast
    continuous AAC encoder such as `libfdk_aac`, or deliberately relax R1 from
    sample-exact segmented AAC to lag/SNR/listening criteria.

### Verification

- `.\.venv\Scripts\python.exe -m pytest tests/test_si_virtual_mp4.py tests/test_config_defaults.py -q`
  -> `22 passed`.
- BOM-aware compile check for `config.py`, `pipeline/si_virtual_mp4.py`,
  `tests/test_si_virtual_mp4.py`, `tests/test_config_defaults.py` -> passed.
- `.\.venv\Scripts\python.exe -m pytest tests/test_si_virtual_mp4.py tests/test_si_mix.py tests/test_config_defaults.py tests/test_routes_media_cache.py -q`
  -> `83 passed`.
- `.\.venv\Scripts\python.exe -m pytest tests/test_content_directory_modes.py -q -k "not versioned_live_id_resolves"`
  -> `34 passed`, `1 deselected`, `4 subtests passed`.
- `git diff --check` -> passed; only CRLF warnings.
- `.\.venv\Scripts\python.exe -m pytest tests -q`:
  - `425 passed`, `2 skipped`, then 4 environment failures:
    - 3 request-history tests cannot append
      `debug_output/request_history/request_history_20260618.jsonl`
      (`PermissionError`).
    - 1 settings test sees current env passthrough mode
      `green,alpha,two_dvr` instead of default `all`.
  - These failures are not from the SI sidecar changes.

## SI Ducking UI Wording

- Updated the SI settings dialog checkbox label:
  - Chinese: `传译时压低原声`
  - English: `Lower original during SI`
  - Japanese: `通訳中に元音声を下げる`
- Added tooltip key `si.duck_original_tooltip` in all three UI translation
  files.
  - Chinese tooltip: `同声传译有声音时，自动降低原片音量。`
- `ui/pages/home_page.py` now applies the tooltip to the SI ducking checkbox.
- Verification:
  - JSON translation files loaded successfully with UTF-8 BOM compatible read.
  - `ui/pages/home_page.py` compile check passed.
  - `git diff --check` passed; only CRLF warnings.
  - `tests/test_settings.py` still has the pre-existing environment-specific
    failure where current passthrough mode is `green,alpha,two_dvr` instead of
    the test default `all`; other tests in that file passed.

## PyInstaller Internal Source Directory Cleanup

### Change

- `build_exe.py`
  - Removed `--add-data tools;tools` and `--add-data offline;offline` from the
    `pt_core` PyInstaller command.
  - Added hidden imports for the runtime tool entry points:
    `tools.offline_passthrough`, `tools.offline_alpha_passthrough`,
    `tools.warmup_offline_trt`, and
    `tools.generate_yoloworld_person_txt_feats`.
  - Added `--collect-submodules offline` so offline Python modules are compiled
    into the PyInstaller module archive instead of copied as original `.py`
    files under `_internal`.
- `pt_core.spec`
  - Synchronized the same packaging rule for direct spec builds.
- `main.py`
  - Added the missing frozen `two_dvr` subcommand dispatch to
    `offline.two_dvr.main()`, matching `ui.services.process_helpers.two_dvr_command()`.

### Verification

- `python -m pytest tests/test_build_exe.py tests/test_main_args.py` ->
  `11 passed`.
- Pytest reported cache-write permission warnings for `.pytest_cache`, but the
  tested packaging and entry-dispatch behavior passed.

### Notes

- This only embeds Python modules. External runtime data, model files, CUDA/ORT
  DLLs, Qt files, and resources still remain outside the exe in the onedir
  package as required by PyInstaller/runtime loading.

## SI Progressive A/V Sync Retest: nPlayer 18:50 Log Review

### User Symptom

- User retested `/media_si` and clarified the audible issue is original program
  audio leading the picture by roughly 1-2 seconds, not SI voice delay relative
  to the original audio.

### Server Log Evidence

- Latest relevant run started at `2026-06-18 18:49:32`.
- Browse prewarm for `videos\SI_TEST_8K.mp4` began at `18:50:04`.
- The SI sidecar used for playback was:
  `runtime_cache\si_virtual_mp4\51eb46d3a757b825621e07e59ed927eced9ab33ae2142df45e9bb11174e79977.audio.mp4`.
- Build parameters from `debug_output/server.log`:
  - `input_kind=source-audio-sidecar`
  - `encoder=aac_mf`
  - `delay=1.0`
  - `mix=both`
  - `orig_vol=100`
  - `si_vol=100`
  - `duck=True`
  - `audio_edit=remove`
- nPlayer playback requests began at `18:50:43` from `192.168.31.110` with
  `ua='nPlayer/3.0'`.
- Observed Range starts included:
  - `bytes=7450853376-`
  - `bytes=3872522240-`
  - `bytes=304414720-`
  - `bytes=403439616-`
  - `bytes=912654336-`

### Local Timing Checks

- `ffprobe` showed source audio, source-audio sidecar, and mixed sidecar all
  have `start_time=0` and duration about `3007.104s`.
- PCM correlation, source MP4 original audio vs
  `f954d753...source-audio.mp4`, at `0/60/600/1500/2500s`:
  - all windows had `content_delta=0.000 ms`, `corr=1.0000`.
  - Therefore the source-audio extraction sidecar did not shift original audio.
- PCM correlation against older native-AAC mixed sidecars stayed around
  `-4.979 ms` at multiple offsets.
- The latest `aac_mf` mixed sidecar was harder to correlate because SI and
  ducking alter the waveform, but most windows did not support a stable 1-2s
  source-audio content shift.

### New Structural Difference Found

- Latest failed test used `encoder=aac_mf`.
- `aac_mf` sidecar audio packet timing is normalized to a single `stts` entry:
  `(140958, 1024)`.
- Source audio, the source-audio sidecar, and old native-AAC sidecars preserve
  source-like initial durations such as `1032, 1024, 1040, ...`.
- This is not by itself enough to explain 1-2 seconds, but it is a real
  structural difference in the exact test path that failed on nPlayer.

### Range/Keyframe Observation

- Mapping the nPlayer Range starts to the virtual MP4 layout showed the starts
  land inside video samples, not on keyframe/sample boundaries.
- Examples:
  - `bytes=3872522240-` landed in video sample `89842` at `1498.864s`,
    offset `31033/37853` bytes into the sample.
  - `bytes=304414720-` landed in video sample `5829` at `97.247s`,
    offset `7895/26756` bytes into the sample.
  - `bytes=912654336-` landed in video sample `25309` at `422.238s`,
    offset `93452/94099` bytes into the sample.
- The source GOP/keyframe spacing averages about `4.85s`.
- For the logged Range starts, the next keyframe was usually close
  (`33-117ms`) except the tail probe (`951ms`), so the keyframe-start theory is
  plausible as a playback mechanism but the specific logged offsets do not
  alone prove a 1-2s offset.

### Code Change Made

- Changed the default SI mixed sidecar encoder from `auto` to native `aac`:
  - `config.py`: `SI_MIX_ENCODER` default is now `"aac"`.
  - invalid env values now fall back to `"aac"`.
  - `aac_mf` remains available via explicit `PT_SI_MIX_ENCODER=aac_mf` for
    speed A/B tests.
- Updated `tests/test_config_defaults.py` to expect `SI_MIX_ENCODER == "aac"`.

### Verification

- `.\.venv\Scripts\python.exe -m pytest tests\test_config_defaults.py tests\test_si_virtual_mp4.py tests\test_si_mix.py tests\test_routes_media_cache.py -q`
  -> `83 passed`.
- Runtime sanity:
  - imported `config.SI_MIX_ENCODER` -> `aac`.
  - `pipeline.si_virtual_mp4._selected_mix_encoder()` -> `aac`.
- `git diff --check` passed with only CRLF warnings for touched files.

### Next Test

- Restart server and retest the same `[SI]` item.
- Confirm `debug_output/server.log` says:
  - `encoder=aac`
  - an `.audio.mp4` digest different from the old `51eb46...` aac_mf file.
- If the issue remains with native `aac`, the next likely branch is player
  seek/decode behavior around non-keyframe Range starts and virtual mdat
  interleaving, not source-audio extraction or SI delay.

### Prebuilt Native-AAC Sidecars

- After changing the default, two native-AAC layouts were prewarmed manually:
  - `delay=0.0`: `f7921701a642be2d992a5c01a7c2d9c3b53ad5f6b97564eb87be9be30426ff82.audio.mp4`
    - `audio_edit=remove`
    - `size=7449678144`
    - `moov=6173403`
    - `samples=180240+140959`
  - `delay=1.0`: `f5eabfd842ecf29d16fcb242c22117c3e250007de539ef076ef3c16d17c21987.audio.mp4`
    - `audio_edit=remove`
    - `size=7449706402`
    - `moov=6173403`
    - `samples=180240+140959`
- `ffprobe` on both sidecars:
  - `start_time=0.000000`
  - `duration=3007.104000`
  - `nb_frames=140959`
- PCM correlation against source original audio:
  - `delay=0.0`: `-4.479ms` at `60/600/1500s`.
  - `delay=1.0`: `-4.479ms` at `60s` and `1500s`; the `600s` window had
    weak correlation from SI/ducking interference and is not conclusive.

### DLNA Browse Size Estimate Fix

- Found that `[SI]` DIDL `size` was badly overestimated:
  - old estimate for `SI_TEST_8K.mp4`: `7,924,036,873`
  - actual native-AAC virtual size: about `7,449,706,402`
  - error: about `+474MB` / `+6.37%`.
- Cause:
  - `_estimated_si_size()` treated SI as `source_size + 192k audio`, then
    multiplied by `1.05`.
  - Progressive SI replaces the source audio track with mixed AAC; it does not
    append another full audio stream.
- Changed `dlna/content_directory.py`:
  - `_estimated_si_size(source_size, duration)` now returns `source_size`.
  - The exact virtual size still comes from `/media_si` HTTP `Content-Length`
    after layout build.
- New estimate for `SI_TEST_8K.mp4`:
  - `7,474,531,284`
  - error vs native-AAC virtual size is now about `+24.8MB` / `+0.33%`.
- Added assertion coverage in `tests/test_si_mix.py` so the SI item keeps
  source-size-based `size` and derived `bitrate`.

## SI Progressive Retest: Quest3 Players Pass, nPlayer Ignored

### User Retest Result

- iPhone nPlayer still reports original audio leading video.
- Quest3 VR headset players all passed with no audible A/V sync issue:
  - SKYBOX
  - DeoVR
  - 4XVR
  - HereSphere
  - MoonVR
- Decision: treat nPlayer as a client-specific compatibility issue and ignore
  it for the current SI progressive route. The target VR playback stack is good.

### Encoder Default Reverted For Performance

- The temporary default switch to native `aac` was reverted.
- `config.py` now defaults back to:
  - `SI_MIX_ENCODER=auto`
  - On Windows, `_selected_mix_encoder()` chooses `aac_mf` when available.
- Rationale:
  - native `aac` is slower for first-play SI sidecar generation.
  - `aac_mf` is the best responsiveness path and has now been validated on the
    Quest3 target players.
  - `PT_SI_MIX_ENCODER=aac` remains available for explicit quality/diagnostic
    A/B tests.
- Kept the DLNA `[SI]` Browse size estimate fix because it improves metadata
  accuracy and does not hurt performance.

### Verification

- `.\.venv\Scripts\python.exe -m pytest tests\test_config_defaults.py tests\test_si_virtual_mp4.py tests\test_si_mix.py tests\test_routes_media_cache.py -q`
  -> `83 passed`; only `.pytest_cache` permission warning.
- Runtime sanity:
  - `config.SI_MIX_ENCODER` -> `auto`
  - `_selected_mix_encoder()` -> `aac_mf`

## SI Help Dialog URL Display

- Updated `ui/pages/home_page.py` so the SI help dialog download link displays
  the full URL text:
  `https://github.com/zerochocobo/VR-Video-Toolbox-CE`
- The link target was already correct; only the visible anchor text previously
  showed `zerochocobo/VR-Video-Toolbox-CE` without the `https://github.com`
  domain.
- Widened the help dialog from `500` to `640` px and the label minimum width
  from `420` to `560` px so the full URL is readable.
- Verification:
  - `git diff --check` passed with only CRLF warnings.
  - Direct source `compile(...)` check passed.
  - `py_compile` could not be used because `ui/pages/__pycache__` is not
    writable in the current workspace.
