# HANDOVER 2026-06-27

## SAM3 / MatAnyone2 offline alpha user report investigation

- User-provided log tail:
  - Source metadata: `codec=hevc profile=Main pix_fmt=yuv420p bit_depth=8 size=8192x4096`.
  - GPU metadata: `NVIDIA GeForce RTX 5090 D`, `cc=12.0`, `vram=31.8GB`, `free=30.3GB`.
  - Decoder decision: `decoder=simple batch=1 buffer=2 owned_copy=False backend_reason=8K+ output should use HEVC; H.264 NVENC failed in probe`.
- Code locations checked:
  - `tools/offline_alpha_passthrough.py`
  - `utils/video_metadata.py`
  - `pipeline/pynv_io.py`
  - `offline/sam3_matanyone2.py`
  - `pipeline/alpha_packer.py`
- Findings:
  - The `backend_reason=8K+ output should use HEVC; H.264 NVENC failed in probe` string is not a runtime error. It is the normal `select_backend()` reason for `resolution_bucket == "8k_plus"` and selects PyNv HEVC output.
  - The pasted lines are emitted by the SAM3 prepass child process when `--sam3-prepass-out` is present.
  - If there are no later lines, the most likely silent wait is before `[offline] SAM3 prepass prompt=...`, where `--sam3-scan hybrid` calls `_probe_keyframe_indices()` and runs `ffprobe -skip_frame nokey -show_entries frame=...` over the whole source without progress or timeout.
  - For `--frames 79970 --fps 59.94125757507171`, the job is about 1334 seconds / 22.2 minutes. Default SAM3 hybrid scanning also adds interval samples at about one sample per second, so the prepass can require roughly 1300+ sample points and two SAM3 passes per sample for SBS eyes.
  - Local workspace has the expected SAM3 and MatAnyone2 model files, but the user install path still needs checking separately.
- Likely user-facing interpretation:
  - This log snippet alone does not show an actual failure.
  - If the process appears stuck immediately after the decoder line, suspect the unlogged keyframe probe or first SAM3 prepass setup.
  - If a later traceback contains `BFCArena::AllocateRawInternal`, `Failed to allocate memory`, `CUDNN`, or `CUDA`, suspect SAM3 CUDA EP memory arena limits or provider compatibility.

