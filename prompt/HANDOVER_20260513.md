# HANDOVER 2026-05-13

## Alpha passthrough subtitle alpha-mask contamination fix

- User reported that when subtitles appear in alpha passthrough video, the original red-channel alpha image becomes the subtitle color and passthrough fails.
- Root cause:
  - subtitles were correctly being blended into the source frame before fisheye alpha packing;
  - however, `overlay_alpha_packer_layout` only wrote alpha layout pixels when `mask != 0`;
  - for `mask == 0`, the kernel returned early and left the already projected visible image underneath;
  - when subtitles overlapped those six alpha layout blocks, their color remained in the alpha control area.
- Updated `pipeline/alpha_packer.py`:
  - alpha layout blocks now always write a clean black / neutral-UV base;
  - nonzero mask pixels still write the red alpha value;
  - this prevents subtitle color from leaking into the red alpha-control regions.
- Retained the previous alpha subtitle flow:
  - blend the v360-projected subtitle into the uploaded source NV12 before fisheye packing;
  - merge subtitle alpha into the alpha mask so subtitle pixels remain opaque to the player.
- Verification:
  - `python -m unittest tests.test_alpha_packer` passed;
  - `python -m py_compile pipeline\alpha_packer.py pipeline\pynv_stream.py` passed;
  - `git diff --check` passed.
- Runtime verification needed:
  - retest alpha live playback with same-stem `.srt` / `.ass`;
  - confirm alpha mask blocks remain red/black and no longer show subtitle color.

### Correction after runtime retest

- User reported the first attempted fix was wrong:
  - subtitle color still polluted the alpha control channel;
  - clearing `mask == 0` alpha layout pixels made the six alpha blocks visibly opaque and covered fisheye corners.
- Corrected the design:
  - keep subtitles out of the uploaded source NV12 used by alpha projection;
  - project the clean source frame first;
  - blend subtitle color only onto the projected fisheye visible layer;
  - merge subtitle opacity into the separate `fisheye_alpha` layer;
  - write the six-block red alpha layout from `fisheye_alpha` after subtitle blending;
  - leave alpha layout pixels unchanged when `mask == 0`, matching the original non-opaque behavior.
- Updated `pipeline/alpha_packer.py`:
  - added a projected subtitle blend kernel that maps subtitle overlay coordinates through the same fisheye transform;
  - the kernel skips the six alpha layout regions so subtitle color cannot write into alpha control blocks;
  - subtitle alpha updates `fisheye_alpha`, so subtitle pixels can still be opaque in the player.
- Updated `pipeline/pynv_stream.py`:
  - alpha mode now passes positioned subtitle overlays to `AlphaPacker`;
  - it no longer blends subtitle color into the uploaded source NV12 before alpha packing.
- Verification:
  - `.venv\Scripts\python.exe -m unittest tests.test_alpha_packer` passed;
  - `.venv\Scripts\python.exe -m py_compile pipeline\alpha_packer.py pipeline\pynv_stream.py` passed.

## 4XVR / Quest Dalvik live Range compatibility

- User provided packet capture showing 4XVR on Quest sends:
  - `User-Agent: Dalvik/2.1.0 ... Quest 3 ...`
  - `Range: bytes=1072693248-1073741823`
  - server returned `416 Requested Range Not Satisfiable` with `accept-ranges: none`.
- Server log matched this:
  - earlier non-Range requests played briefly then disconnected;
  - the later Range request hit the default `vlc` profile and returned 416;
  - a following request got blocked behind the still-active live stream and returned 503.
- Updated `http_app/routes_media.py`:
  - classify `Dalvik/` + `Quest` as `quest_dalvik`;
  - route `quest_dalvik` through managed live-session behavior like libmpv/Skybox;
  - do not return 416 for nonzero Range requests from this profile;
  - allow same-device `quest_dalvik` requests to preempt stale live owners;
  - when a cached live session has no primary subscriber, a cache hit can become the primary subscriber instead of receiving only a snapshot.
- Added `tests/test_routes_media_cache.py` coverage for:
  - Quest Dalvik profile detection;
  - existing known profile precedence;
  - same-device preemption for `quest_dalvik`.
- Verification:
  - `.venv\Scripts\python.exe -m unittest tests.test_routes_media_cache tests.test_alpha_packer` passed;
  - `.venv\Scripts\python.exe -m py_compile http_app\routes_media.py pipeline\alpha_packer.py pipeline\pynv_stream.py` passed.

## RVM model download readme

- Added `models/get_rvm_model_readme.txt`.
- The file contains English and Chinese instructions for downloading:
  - `rvm_mobilenetv3_fp32.onnx`;
  - `rvm_resnet50_fp32.onnx`.
- It points to:
  - the official Robust Video Matting GitHub project;
  - the official GitHub release `v1.0.0`;
  - direct GitHub release asset links;
  - a SourceForge mirror for the same release.
- UTF-8 content sanity check passed:
  - `.venv\Scripts\python.exe -c "... read_text(encoding='utf-8') ..."` confirmed Chinese markers and both model names are present.

## README supported VR players table

- Added supported VR player compatibility tables to both README files:
  - `README.md`;
  - `README.zh-CN.md`.
- Inserted the section after the launch instructions and before configuration notes.
- Table columns:
  - player;
  - alpha passthrough;
  - gray green screen;
  - ChromaKey green screen;
  - website;
  - notes.
- Normalized the Markdown table format and fixed the Moon Player URL to `https://moonvrplayer.com`.
- Verified both README files still have UTF-8 BOM.

## README passthrough output examples

- Added passthrough output example sections to both README files:
  - `README.md`: `Passthrough Output Examples`;
  - `README.zh-CN.md`: `透视视频效果图`.
- Inserted each section after the feature list and before the runtime requirements.
- The sections use a two-column Markdown table:
  - Alpha Passthrough -> `resources/sample_alpha.jpg`;
  - Green-screen / 绿幕 Passthrough -> `resources/sample_green.jpg`.
- Verified both README files still have UTF-8 BOM.

## README project origin section

- Added a project origin section above the feature list in both README files:
  - `README.md`: `Project Origin`;
  - `README.zh-CN.md`: `项目起源`.
- Used standard Markdown `## ` heading spacing.
- Kept both files as UTF-8 BOM.

## Japanese README

- Added `README.ja-JP.md`, a Japanese translation of the current README content.
- Added three-way language navigation at the top of:
  - `README.md`;
  - `README.zh-CN.md`;
  - `README.ja-JP.md`.
- The English README's Japanese entry text is `日本語`, per user request.
- Updated the README notes section to link to the Japanese version where appropriate.
- Verified all three README files are UTF-8 BOM.
