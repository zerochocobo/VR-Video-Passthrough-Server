# HANDOVER 2026-07-16

## RM Entry Reopened And Chunk Restoration Model Integrated

The user requested:

1. Reopen the Home-page mosaic restoration entry.
2. Replace the mosaic restoration model with `models/demosaic/vr_mosaic_restoration_chunk_model_v0.1.onnx`.

The user also supplied the model provider notes:

- Input `lqs`: `(B, 8, 3, 256, 256)`, RGB normalized to `[0, 1]`.
- Input `prev_ds`: `(B, 3, 64, 64)`.
- Input `states`: `(B, 2, 64, 64, 64)`.
- Output `restored`: `(B, 8, 3, 256, 256)`.
- Output `ds_out` / `states_out` feed the next chunk.
- Initial or new-region state: `states = 0`; `prev_ds` is the first frame downsampled to `64x64`.
- Run once per 8-frame chunk; multiple mosaic regions use the batch axis.
- Clamp restored output to `[0, 1]` before converting to uint8.

Reference source paths supplied by the user:

- `G:\GIT\lada\configs\basicvsrpp\mosaic_restoration_generic_vr_chunk_stage1.py`
- `G:\GIT\lada\export_restoration_chunk_onnx.py`

## Changes

- `pipeline/demosaic.py`
  - Switched the restoration model file to `vr_mosaic_restoration_chunk_model_v0.1.onnx`.
  - Changed RM `WINDOW` from `7` to `8`.
  - Changed TensorRT cache bucket from `restoration` to `restoration_chunk` so stale window-model engines are not treated as valid.
  - Added chunk-model input/output handling for `lqs`, `prev_ds`, `states`, `restored`, `ds_out`, and `states_out`.
  - Added initial feedback generation from the first frame downsampled to `64x64`.
  - Added zero-state initialization for new regions.
  - Added GPU chunk processing through `GpuRmProcessor.process_chunk()`.
  - Batched multiple detected mosaic regions on the restoration model batch axis.
  - Added IoU-based region-state reuse across chunks, with new/changed regions starting from fresh feedback.
  - Kept `GpuRmProcessor.process()` as a compatibility helper returning the representative center output from one chunk.
- `pipeline/pynv_stream.py`
  - Reworked realtime RM streaming from per-frame sliding-window inference to 8-frame chunk inference.
  - Encodes only the actual requested frame count from each padded final chunk.
  - Updated RM startup logging from `window/center` wording to `chunk`.
- `offline/demosaic_offline.py`
  - Reworked offline RM conversion to process 8-frame chunks and encode the restored frames from each chunk.
  - Removed old center-window preroll from the decoder start frame.
- `ui/main_window.py`
  - Reinstated `RmPage` and `RmProcess`.
  - Reconnected the Home-page offline RM button route.
  - Re-added RM page stack wiring, retranslation, running-state handling, and shutdown cleanup.
- `ui/pages/home_page.py`
  - Reopened the RM row by removing the hidden-row call.
  - Re-added the RM label to Home config label alignment/width calculations.
- `ui/settings.py`
  - Removed the one-time migration that forced previously saved `rm_enabled: true` to `False`; saved user preference is preserved now that the entry is visible again.
  - Default `rm_enabled` remains `False`.
- `tests/test_settings.py`
  - Replaced the old migration-off assertion with a saved-enabled-preserved assertion.
- `tests/test_ui_smoke.py`
  - Updated Home RM assertions: RM row is visible, default setting remains off, and the main window stack count is back to 5.
- `tests/test_demosaic_chunk_model.py`
  - Added a regression test that asserts `WINDOW == 8` and the chunk ONNX file is selected.
- `tools/bench_rm.py`
  - Updated text from 7-frame window wording to 8-frame chunk wording.

## Verification

- AST validation:
  - `pipeline/demosaic.py`
  - `pipeline/pynv_stream.py`
  - `offline/demosaic_offline.py`
  - `ui/main_window.py`
  - `ui/pages/home_page.py`
  - `tests/test_ui_smoke.py`
  - `tests/test_settings.py`
  - `tests/test_demosaic_chunk_model.py`
  - `tools/bench_rm.py`
  - Result: passed
- Model path / availability check:
  - `WINDOW=8`, `CENTER=4`
  - Restoration model path resolved to `models\demosaic\vr_mosaic_restoration_chunk_model_v0.1.onnx`
  - `models_available()` returned `True`
- CPU ONNX Runtime interface check:
  - Created `DemosaicRestorer(provider="cpu")`
  - Ran one zero chunk through `restore_chunk_stack()`
  - Output shapes:
    - `restored`: `(1, 8, 3, 256, 256)`
    - `ds_out`: `(1, 3, 64, 64)`
    - `states_out`: `(1, 2, 64, 64, 64)`
  - Restored output range before clamp was small around zero, confirming the output can be clamped as expected.
- `.\.venv\Scripts\python.exe -B -m pytest tests\test_demosaic_chunk_model.py tests\test_ui_smoke.py`
  - 4 passed
- `.\.venv\Scripts\python.exe -B -m pytest tests\test_settings.py -k "rm or server_env_omits_blank_subtitle_color"`
  - 2 passed
- `.\.venv\Scripts\python.exe -B -m pytest tests\test_content_directory_modes.py`
  - 40 passed
- `git diff --check`
  - Passed. Git only warned that touched working-tree files will be converted from LF to CRLF the next time Git touches them.

## Notes

- `config.py` still defaults `PT_RM_ENABLED` to off (`0`). The entry is visible again, but users must explicitly enable realtime RM.
- Old TensorRT engines under `runtime_cache/demosaic_trt/restoration` are not reused because the chunk model now uses `runtime_cache/demosaic_trt/restoration_chunk`.
- Current `git status --short` includes unrelated pre-existing changes such as `ui/app_metadata.json`; those were not part of this RM work.

## Main-window demosaic entry visibility switch

- Added `SHOW_DEMOSAIC_CONFIG_ROW` to `ui/main_window.py`, directly below the startup sizing/timing constants and above `MainWindow`.
- The switch defaults to `True`, preserving the current UI.
- Set it to `False` to hide the entire demosaic configuration row on the home page, including the realtime enable switch and offline generation entry.
