# Handover — 2026-07-18

本轮用户要求：规划 NVIDIA RTX VSR 实时/离线超分功能，输出使用 `[SuperRes]` 前缀，并要求 SDK 不直接引用 `reference`，打包时复制到 exe 目录。

已完成：

- 梳理项目结构：实时主链路为 `pipeline/pynv_stream.py`（PyNvVideoCodec/CuPy/NVENC），离线入口为 `offline/convert.py`，打包入口为 `build_exe.py` 和两个 `.spec`。
- 检查 RTX Video SDK v1.1.0：包含 `nvngx_vsr.dll`、NGX 头文件、DX11/DX12/CUDA VSR helper 与 samples；VSR 质量级别 1–4 可作为用户配置。
- 形成开发计划（中英文）：
  - `summary/summary_20260718_RTX_VSR_DEVELOPMENT_PLAN_CN.md`
  - `summary/summary_20260718_RTX_VSR_DEVELOPMENT_PLAN_EN.md`

补充 PDF 复核结论：Programming Guide 没有声明 360p–1440p 是 SDK 硬限制，也没有声明只支持 2D。第 46–51 页定义的是 RGB SDR surface、输入/输出矩形和质量级别；示例含 480p→1080p 和 1080p 输入。项目首版仍可保守限制为 360p–1440p 普通 2D，但日志必须标识为项目策略，4K/VR 待 native POC 验证；输出标记统一为 `[SuperRes]`。

新增复核报告：

- `summary/summary_20260718_RTX_VSR_OFFICIAL_SCOPE_REVIEW_CN.md`
- `summary/summary_20260718_RTX_VSR_OFFICIAL_SCOPE_REVIEW_EN.md`
- `summary/summary_20260718_RTX_VSR_PROGRAMMING_GUIDE_REVIEW_CN.md`
- `summary/summary_20260718_RTX_VSR_PROGRAMMING_GUIDE_REVIEW_EN.md`

建议下一步：先做 Windows x64 native bridge 的普通 2D 单帧 capability/interop POC，再接实时链路、离线链路和 PyInstaller 打包；本轮未修改功能代码。

目录调整：用户要求 RTX VSR 资产统一由 `models/rtx_vsr` 管理。当前结构为 `models/rtx_vsr/native`（bridge 源码、SDK headers/lib、构建脚本）和 `models/rtx_vsr/runtime`（bridge DLL、`nvngx_vsr.dll`、许可证与版本文件），不再使用根目录 `native/rtx_vsr_bridge` 或 `resources/rtx_video_sdk`。

实施进展：

- 已完成 Python `ctypes` bridge（`utils/rtx_vsr.py`），配置项、`[SuperRes]` 命名、离线 `rtx_vsr` engine 和实时 `mode=superres` PyNv 分支。
- `build_exe.py`、两个 spec 已加入 `models/rtx_vsr/runtime`，终端用户不需要编译器；发布包包含预编译 bridge、`nvngx_vsr.dll`、对应 `cudart64_12.dll`、许可证和版本文件。
- SDK 官方 DX11 sample 在本机 RTX 5060 Ti / driver 581.57 上初始化成功。
- CUDA bridge 用 CUDA 12.6 编译后 capability probe 成功；用 CUDA 13 编译的 bridge 在 NGX CUDA init 阶段失败，当前应按 CUDA 12.x 兼容性继续验证。首帧 evaluate 仍需完成性能/模型加载验证。
- 已通过 RTX VSR 相关单测、命名/离线/打包测试（当前测试集合 31 passed；后续完整回归仍需执行）。

## 2026-07-19 continuation

- 新增 `RTX_VSR_EVALUATE_TIMEOUT_SEC` 配置（默认 45 秒）。
- 新增 `utils.rtx_vsr.run_evaluation_preflight()`：在独立子进程中执行真实单帧 NGX VSR evaluate，并在超时时终止子进程。
- 主程序启动时（默认输出模式包含 `superres` 时）执行预检；实时请求在尚未预检时也会惰性预检。预检失败会拒绝实时 SuperRes，避免 PyNv worker 永久阻塞。
- `main.py tool rtx_vsr_probe` 已加入冻结 exe 的工具分发路径。
- `models/rtx_vsr/native/build.ps1` 默认 CUDA 编译根目录改为 CUDA 12.6；CUDA 13 仍不作为发布构建配置。
- 当前环境可运行 RTX VSR 单测；UI smoke 测试因环境未安装 PySide6 未执行通过。真实 NGX evaluate 仍受 SDK/驱动在本机阻塞问题影响，不能宣称功能已完工。
