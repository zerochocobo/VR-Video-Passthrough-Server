## 2026-05-23 Track A first-chunk latency A6/A7

- User provided expert patch document:
  - `summary/summary_20260523_FIRST_CHUNK_LATENCY_PATCH_CN.md`
- Log diagnosis before patch:
  - Track A GPU/NVENC warmup was healthy: static TRT preload, CuPy composite/alpha warmup, and NVENC startup preflight all completed.
  - First-playback FPS targets were met: frame 30 `54.92`, frame 60 `59.92`, frame 120 `68.13`, frame 600 `76.49`, steady `77-78fps`.
  - Residual startup delay was between mux startup/first bitstream and reader first stdout chunk, about `1.7-1.8s`, matching FFmpeg stdin probe delay rather than matting or NVENC init.
- Implemented A6 diagnostics:
  - Added `PT_MUX_LATENCY_DIAG=1` default.
  - `PyNvPassthroughStream` now records first-chunk marks:
    - `T0_video_mux_spawn`
    - `T0_mux_spawn`
    - `T1_first_write`
    - `T2_first_stderr`
    - `T4_reader`
  - Reader first chunk now logs `[DIAG][MUX][sid] first_chunk_breakdown ...`.
- Implemented A6.5 diagnostic switch:
  - Added `PT_FORCE_AUDIO_OFF=1` to force video-only mux output for pipe_ts vs single-stage comparison.
- Implemented A7 low-latency mux parameters with rollback envs:
  - Added `PT_MUX_PROBESIZE_OVERRIDE`, default `32`.
  - Added `PT_MUX_ANALYZEDURATION_US`, default `0`.
  - Added `PT_MUX_NOBUFFER_ENABLE`, default `1`.
  - Added `PT_FMP4_FRAG_DURATION_US`, default `100000`.
  - Applied probe/analyzeduration args to direct mux video input, direct mux audio/file input, pipe_ts video input, pipe_ts final mpegts input, pipe_ts audio input, and slate pipe_ts equivalents.
  - Replaced FFmpeg `-fflags +genpts` with configurable `+genpts+nobuffer+flush_packets` when enabled.
  - Changed fMP4 `-frag_duration` from hardcoded `250000` to configurable default `100000`.
- Tests:
  - `uv run python -m pytest tests\test_config_defaults.py tests\test_pynv_mux_latency.py tests\test_pynv_startup_preflight.py` -> `8 passed`.
  - `uv run python -m pytest tests\test_config_defaults.py tests\test_predict_warmup_state.py tests\test_alpha_packer.py tests\test_vr_naming.py tests\test_pynv_startup_preflight.py tests\test_pynv_mux_latency.py tests\test_main_args.py` -> `38 passed`.
  - `python -m compileall config.py pipeline\pynv_stream.py tests\test_config_defaults.py tests\test_pynv_mux_latency.py` passed.
  - `git diff --check` produced only CRLF normalization warnings for touched files.
- Next manual validation:
  - Start server normally and play the same alpha MPEG-TS item.
  - Check `server.log` for mux commands containing `-probesize 32 -analyzeduration 0` and `-fflags +genpts+nobuffer+flush_packets`.
  - Check `[DIAG][MUX] first_chunk_breakdown`; target is reader total `<800ms` after A7.1+.
  - If total remains >1s, run with `PT_FORCE_AUDIO_OFF=1` to isolate double-mux/audio interleave.

## 2026-05-23 A7 first live log and probe split fix

- User ran playback after A7. Latest `debug_output/server.log` showed:
  - A7 command flags were active in both pipe_ts processes.
  - `T1_first_write=176.8ms`, `T2_first_stderr=300.2ms`, but `T4_reader=1908.9ms`.
  - Therefore raw HEVC stdin probing was not the remaining bottleneck.
  - FFmpeg final mux logged `Could not find codec parameters ... unspecified size` and `Consider increasing ... analyzeduration/probesize`, which means applying `-probesize 32` to the final-stage MPEG-TS input was too aggressive.
- Follow-up code change:
  - Kept raw HEVC/H264 stdin `PT_MUX_PROBESIZE_OVERRIDE=32`.
  - Added `PT_MUX_CONTAINER_PROBESIZE_OVERRIDE=32768` for pipe_ts final-stage MPEG-TS stdin.
  - Added `PT_MUX_AUDIO_PROBESIZE_OVERRIDE=32768` for AAC/file/tcp audio inputs.
  - Updated direct mux fallback audio/container input probe selection accordingly.
- Verification after follow-up:
  - `python -m compileall config.py pipeline\pynv_stream.py tests\test_config_defaults.py tests\test_pynv_mux_latency.py` passed.
  - `uv run python -m pytest tests\test_config_defaults.py tests\test_pynv_mux_latency.py tests\test_pynv_startup_preflight.py` -> `8 passed`.
- Next validation should confirm final mux cmd has:
  - video raw stdin: `-probesize 32`
  - final mpegts stdin: `-probesize 32768`
  - audio aac input: `-probesize 32768`
  - no `unspecified size` warning before first chunk.

## 2026-05-23 pipe_ts final mux verdict and setts default

- User played again after probe split. Latest log showed:
  - video raw stdin used `-probesize 32`;
  - final mpegts stdin and audio aac input used `-probesize 32768`;
  - `first_chunk_breakdown` was still `T1_write=190.7ms T2_stderr=298.4ms T4_reader=1909.5ms total=1909.5ms`;
  - final mux still logged `Could not find codec parameters ... unspecified size`.
- Verdict:
  - The remaining 1.9s is not solved by stdin probesize tuning.
  - The double-stage `pipe_ts` final mux is still waiting for parseable HEVC codec parameters from the middle TS stream.
- Follow-up change:
  - Changed default `PT_PASSTHROUGH_AUDIO_MPEGTS_TIMESTAMP_MODE` from `pipe_ts` to `setts`.
  - This uses the existing single-stage mux path with AAC cache and avoids `video_proc -> final_proc` probing.
  - Fixed single-stage setts timestamp tick to derive from actual fps instead of the old hardcoded `3000` tick (30fps).
  - Added `_mpegts_tick_for_fps()` with an NTSC 59.94 tolerance returning `1502`.
  - `pipe_ts` remains available as rollback via `PT_PASSTHROUGH_AUDIO_MPEGTS_TIMESTAMP_MODE=pipe_ts`.
- Verification:
  - `uv run python -m pytest tests\test_config_defaults.py tests\test_predict_warmup_state.py tests\test_alpha_packer.py tests\test_vr_naming.py tests\test_pynv_startup_preflight.py tests\test_pynv_mux_latency.py tests\test_main_args.py` -> `39 passed`.
  - `git diff --check` only CRLF warnings.

## 2026-05-23 audio-mode regression root cause fix

- User reported that from one of today's builds, real players entered audio-only mode instead of video mode; `tests\dlna_client\app.py` did not reveal it.
- Expert analysis matched the logs:
  - raw HEVC stdin was using `-probesize 32 -analyzeduration 0`;
  - FFmpeg could not reliably parse VPS/SPS/PPS from raw HEVC;
  - the intermediate/final MPEG-TS lacked usable video codec parameters;
  - strict players then classified the stream as audio-only.
- Implemented the recommended fix:
  - `_mux_probe_args(..., for_raw_video=True)` returns no probe/analyze args.
  - All raw HEVC/H264 stdin inputs now call `_mux_probe_args(for_raw_video=True)`:
    - pipe_ts video mux input;
    - slate pipe_ts video mux input;
    - direct mux video-only input;
    - direct mux audio-enabled primary video input.
  - Container/audio inputs still use `PT_MUX_CONTAINER_PROBESIZE_OVERRIDE=32768` and `PT_MUX_AUDIO_PROBESIZE_OVERRIDE=32768`.
  - Default remains stable `PT_PASSTHROUGH_AUDIO_MPEGTS_TIMESTAMP_MODE=pipe_ts`.
- Tests updated:
  - `tests/test_pynv_mux_latency.py` now asserts raw video inputs do not include `-probesize` or `-analyzeduration`, while final/audio inputs still use 32768.
- Verification:
  - `python -m compileall config.py pipeline\pynv_stream.py tests\test_config_defaults.py tests\test_pynv_mux_latency.py` passed.
  - `uv run python -m pytest tests\test_config_defaults.py tests\test_predict_warmup_state.py tests\test_alpha_packer.py tests\test_vr_naming.py tests\test_pynv_startup_preflight.py tests\test_pynv_mux_latency.py tests\test_main_args.py` -> `39 passed`.
  - `git diff --check` only CRLF warnings.
- Next validation:
  - Real player logs should show `pipe_ts video mux cmd` raw `-f hevc ... -i -` with no `-probesize`/`-analyzeduration` before that first `-i`.
  - `pipe_ts final mux cmd` should still show `-probesize 32768 -analyzeduration 0` for mpegts/audio inputs.
  - nPlayer/Quest/SKYBOX should enter video playback mode rather than audio-only mode.
- Next validation:
  - New log should show `[PYNV][sid] mux cmd:` rather than `pipe_ts video/final mux cmd`.
  - The command should include `setts=time_base=1/90000:pts=N*1502:dts=N*1502`.
  - Check `[DIAG][MUX] first_chunk_breakdown`; target is now total well below the old `1909ms`.

## 2026-05-23 single-stage setts rollback

- User reported playback opened but no content appeared / startup stuck after switching default to single-stage `setts`.
- Log showed single-stage `setts` did produce first chunks, for example:
  - `first_chunk_breakdown ... total=1118.2ms` on the short test item;
  - later seek requests produced first chunks in `2400-3800ms`, largely due to first decode after seek.
- However bytes emitted stayed extremely low relative to frames, e.g. stream sent only hundreds of KB while processing thousands of frames, and clients repeatedly preempted/retried.
- Verdict:
  - Single-stage `setts` is not compatible with current nPlayer/SKYBOX live alpha path even though it emits an initial chunk.
  - It must remain experimental only.
- Code rollback/fix:
  - Default `PT_PASSTHROUGH_AUDIO_MPEGTS_TIMESTAMP_MODE` restored to `pipe_ts`.
  - Single-stage `setts` fps tick fix remains in code for experiments.
  - Fixed cleanup-path `UnboundLocalError: slate_stop` by initializing `slate_stop` and `slate_thread` at `_worker_loop` entry before any early failure path.
- Verification:
  - `uv run python -m pytest tests\test_config_defaults.py tests\test_predict_warmup_state.py tests\test_alpha_packer.py tests\test_vr_naming.py tests\test_pynv_startup_preflight.py tests\test_pynv_mux_latency.py tests\test_main_args.py` -> `39 passed`.
  - `git diff --check` only CRLF warnings.

## 2026-05-23 audio-only regression follow-up: final mux TS probe

- User retested after raw HEVC probe removal; nPlayer still entered audio-only mode.
- Latest `debug_output/server.log` showed raw video input was already fixed:
  - `pipe_ts video mux cmd ... -f hevc ... -i -` had no `-probesize` or `-analyzeduration`.
- But final mux still logged:
  - `PPS id out of range: 0`;
  - `Skipping invalid undecodable NALU: 19`;
  - `Could not find codec parameters for stream 0 (Video: hevc ..., none): unspecified size`;
  - FFmpeg explicitly referenced `analyzeduration (0)` and `probesize (32768)`.
- Additional probe:
  - `tools\pynv_encode_probe.py --codec hevc --width 1280 --height 720 ... --force-idr-first` produced a valid HEVC stream.
  - A quick Annex-B parser confirmed first packet NAL types `32,33,34,19,1,1` (VPS/SPS/PPS before IDR/slices).
  - 8192x4096 synthetic HEVC also ffprobed with correct width/height.
- Verdict:
  - PyNv/NVENC can emit VPS/SPS/PPS correctly.
  - The remaining strict-player audio-only classification is caused by the pipe_ts final mux applying low probe/analyze limits to the intermediate MPEG-TS stdin, then emitting final TS with video codec params missing/unspecified.
- Code change:
  - Added `_mux_intermediate_ts_probe_args()` returning no `-probesize`/`-analyzeduration`.
  - `_open_pipe_ts_muxer()` and `_open_slate_pipe_ts_muxer()` now use default FFmpeg probing for intermediate MPEG-TS stdin only.
  - Raw HEVC remains default probing via `_mux_probe_args(for_raw_video=True)`.
  - AAC/audio input keeps `PT_MUX_AUDIO_PROBESIZE_OVERRIDE=32768` and `PT_MUX_ANALYZEDURATION_US=0`.
- Tests:
  - Updated `tests/test_pynv_mux_latency.py` to assert pipe_ts final mux has no low probe args before the intermediate TS input, but still uses one `32768/0` probe pair for AAC.
  - `python -m compileall config.py pipeline\pynv_stream.py tests\test_pynv_mux_latency.py tests\test_config_defaults.py` passed.
  - `uv run python -m pytest tests\test_config_defaults.py tests\test_pynv_mux_latency.py tests\test_pynv_startup_preflight.py tests\test_main_args.py` -> `15 passed`.
- Next validation:
  - New `pipe_ts final mux cmd` should show `-thread_queue_size ... -f mpegts -i -` with no `-probesize`/`-analyzeduration` before that TS input.
  - It should still show `-probesize 32768 -analyzeduration 0 -f aac ...` for AAC input.
  - Logs should no longer show `Video: hevc ..., none` / `unspecified size`; nPlayer should enter video mode.

## 2026-05-23 audio-only regression follow-up: remove HEVC AUD bsf from setts stage

- User retested after intermediate TS default probing; nPlayer still entered audio-only mode.
- New log confirmed the code change was active:
  - final mux command had `-thread_queue_size 1024 -f mpegts -i -` with no low probe args before the TS input.
  - FFmpeg nevertheless still logged `Could not find codec parameters ... Video: hevc ..., none`.
  - The warning now referenced FFmpeg defaults: `probesize (5000000)`, proving the failure is no longer a too-small probe limit.
- Synthetic PyNv HEVC probe under production-like encoder options (`P1`, `ultra_low_latency`, `rc=cbr`, 8192x4096) produced valid Annex-B HEVC:
  - first NAL types were `32,33,34,19,1,1`;
  - ffprobe recognized `width=8192 height=4096`.
- New hypothesis:
  - The production `video_proc` bsf chain used `hevc_metadata=aud=insert,setts=...`.
  - `hevc_metadata` forces FFmpeg's HEVC parser to decode parameter sets while copying raw stdin to MPEG-TS; the logs show parser failures before output codec params are established.
  - AUD insertion is not required for player video classification; timestamps are required, so only keep `setts` on the pipe_ts video stage.
- Code change:
  - `_mpegts_video_bsf(timestamp_filter=...)` now omits `hevc_metadata=aud=insert` when a timestamp filter is present.
  - Direct MPEG-TS paths without `setts` can still use AUD insertion if `PT_PASSTHROUGH_MPEGTS_HEVC_AUD=1`.
  - Added `_hevc_nal_summary()` and first-bitstream logging:
    - `first bitstream len=... hevc_nals=...`
    - A healthy HEVC start should include `32`, `33`, `34` before `19`.
- Tests:
  - Added mux test asserting pipe_ts video mux bsf contains `setts` but not `hevc_metadata=aud=insert`.
  - Added NAL summary unit test.
  - `python -m compileall pipeline\pynv_stream.py tests\test_pynv_mux_latency.py` passed.
  - `uv run python -m pytest tests\test_pynv_mux_latency.py tests\test_config_defaults.py tests\test_pynv_startup_preflight.py` -> `11 passed`.
- Next validation:
  - New `pipe_ts video mux cmd` should show `-bsf:v setts=time_base=1/90000:pts=N*1502:dts=N*1502`.
  - It should not show `hevc_metadata=aud=insert,setts=...` in that command.
  - Check first bitstream log: expected `hevc_nals=32:...,33:...,34:...,19:...`.
  - If first bitstream has 32/33/34 but FFmpeg still reports PPS failures, next step is to bypass first-stage FFmpeg copy muxing and write raw HEVC through a controlled local probe/output capture.

## 2026-05-23 nPlayer audio-only regression resolved

- User confirmed playback finally enters video mode after removing `hevc_metadata=aud=insert` from the pipe_ts video-stage bsf chain.
- Latest successful log:
  - `pipe_ts video mux cmd` now has only `-bsf:v setts=time_base=1/90000:pts=N*1502:dts=N*1502`.
  - `pipe_ts final mux cmd` keeps default probing for intermediate TS stdin: `-f mpegts -i -`, then low probe only for AAC: `-probesize 32768 -analyzeduration 0 -f aac`.
  - No more `PPS id out of range`, `Skipping invalid undecodable NALU`, or `Video: hevc ..., none`.
  - Remaining warning is only `[aac] Estimating duration from bitrate`, which is not playback-fatal.
- Root cause:
  - `hevc_metadata=aud=insert,setts=...` forced FFmpeg's HEVC parser in the video-stage pipe.
  - In the live pipe_ts production chain this led to missing/invalid HEVC codec params in the intermediate/final TS.
  - Strict players then classified the stream as audio-only.
- Summary written:
  - `summary/summary_20260523_NPLAYER_AUDIO_ONLY_REGRESSION_CN.md`
- Keep/avoid going forward:
  - Keep Track A warmups, NVENC preflight, alpha black slate, fisheye projection fix.
  - Keep default `pipe_ts`; do not default back to single-stage `setts`.
  - Do not put low `-probesize 32 -analyzeduration 0` on raw HEVC stdin.
  - Do not put low probe/analyze on intermediate TS stdin.
  - Do not use `hevc_metadata=aud=insert` in pipe_ts video stage when `setts` is present.
  - First-chunk latency remains ~2.7-3.9s in latest logs, but correctness now has priority.

## 2026-05-23 FastAPI startup deprecation warning fixed

- User reported `DeprecationWarning: on_event is deprecated`.
- Source:
  - `main.py` dynamically registered `app.on_event("startup")(ready_signal)`.
  - `http_app/server.py` used `@app.on_event("startup")` for the asyncio noise filter.
- Code change:
  - `http_app.server.create_app()` now accepts an optional `startup_hook`.
  - The app uses FastAPI lifespan via `asynccontextmanager`.
  - Lifespan installs the asyncio disconnect-noise filter and awaits the optional startup hook when needed.
  - `main.py` now passes `_StartupReadySignal(startup_step_total)` into `create_app(startup_hook=...)`.
  - The ready signal now publishes the final `listening` step with `step_index/step_total`.
- Checks:
  - `rg -n "on_event|add_event_handler" main.py http_app tests` no longer finds runtime app registrations.
  - `python -m compileall main.py http_app\server.py tests\test_main_args.py tests\test_predict_warmup_state.py tests\test_light_match_control.py` passed.
  - `uv run python -m pytest tests\test_main_args.py tests\test_predict_warmup_state.py tests\test_light_match_control.py` -> `21 passed`.

## 2026-05-23 A8.1 first-chunk latency split-stage diagnostic landed

- Implemented expert A8.1 from `summary/summary_20260523_FIRST_CHUNK_LATENCY_A8_1_PATCH_CN.md`.
- Code changes:
  - `pipeline/pynv_stream.py` keeps old `T2_first_stderr` / `T2_stderr` compatibility.
  - Added split marks:
    - `T2a_video_first_stderr` for `ffmpeg-video`.
    - `T2b_final_first_stderr` for final `ffmpeg`.
    - `T3a_final_video_codec`, `T3b_final_audio_codec`, `T3c_final_output_ready` from final mux stderr text.
  - `first_chunk_breakdown` now logs `T2a_video`, `T2b_final`, `T3a_vcodec`, `T3b_acodec`, `T3c_output`.
  - `config.py` adds `PT_MUX_LATENCY_DIAG_VERBOSE`, default `1`; when set to `0`, T3 marks are disabled but T2a/T2b remain.
- Guardrails preserved:
  - No FFmpeg mux command-line changes.
  - No raw HEVC/intermediate TS low-probe changes.
  - No `hevc_metadata=aud=insert` reintroduction in pipe_ts video stage.
- Tests:
  - `python -m compileall config.py pipeline\pynv_stream.py tests\test_pynv_mux_latency.py tests\test_config_defaults.py` passed.
  - `uv run python -m pytest tests\test_pynv_mux_latency.py tests\test_config_defaults.py tests\test_pynv_startup_preflight.py` -> `13 passed`.
- Next validation:
  - Restart server and play once.
  - Inspect `server.log` for `T2a_video_first_stderr`, `T2b_final_first_stderr`, `T3a_final_video_codec`, `T3b_final_audio_codec`, `T3c_final_output_ready`, and the expanded `first_chunk_breakdown`.
  - Use the A8.1 decision tree to choose A8.2/A8.3/A8.x only after real deltas are available.

## 2026-05-23 A8.1b temporary FFmpeg loglevel switch for T3 collection

- Latest server log with A8.1 showed:
  - `T2b_final_first_stderr=2713.7ms`
  - `T4_reader=2725.5ms`
  - `T3a/T3b/T3c=-1.0`
- Reason: PyNv FFmpeg commands still used `-loglevel warning`, so normal `Stream #...` and `Output #0` lines were not emitted.
- Code change:
  - `config.py` adds `PT_MUX_FFMPEG_LOGLEVEL`, default `warning`.
  - `pipeline/pynv_stream.py` uses `_mux_loglevel()` for PyNv FFmpeg command `-loglevel` values.
  - For one diagnostic run, set `PT_MUX_FFMPEG_LOGLEVEL=info`; after collecting data, unset it or set it back to `warning`.
- Tests:
  - `python -m compileall config.py pipeline\pynv_stream.py tests\test_pynv_mux_latency.py tests\test_config_defaults.py` passed.
  - `uv run python -m pytest tests\test_pynv_mux_latency.py tests\test_config_defaults.py tests\test_pynv_startup_preflight.py` -> `14 passed`.

## 2026-05-23 A8.P1 raw video probe override entry landed

- Based on expert `summary/summary_20260523_FIRST_CHUNK_LATENCY_A8_2_PATCH_CN.md`.
- Latest successful A8.1 info-log data:
  - `T1_write=182.1ms`
  - `T2a_video=984.7ms`
  - `T2b_final=2750.5ms`
  - `T3a_vcodec=2751.8ms`
  - `T3b_acodec=2754.0ms`
  - `T3c_output=2758.2ms`
  - `T4_reader=2821.3ms`
  - Conclusion: final mux codec/audio/output phases are fast; dominant delay is raw HEVC probe plus video-proc-to-final-mux waiting.
- Code change:
  - `config.py` adds `PT_MUX_RAW_VIDEO_PROBESIZE` and `PT_MUX_RAW_VIDEO_ANALYZEDURATION`, both default empty.
  - `_mux_probe_args(for_raw_video=True)` injects `-probesize` / `-analyzeduration` only when these env vars are set.
  - Default behavior remains baseline: raw HEVC stdin has no explicit probe/analyze flags.
- Tests:
  - `python -m compileall config.py pipeline\pynv_stream.py tests\test_pynv_mux_latency.py tests\test_config_defaults.py` passed.
  - `uv run python -m pytest tests\test_pynv_mux_latency.py tests\test_config_defaults.py tests\test_pynv_startup_preflight.py` -> `15 passed`.
- First experiment to run:
  - User needs UI-driven testing, so defaults were temporarily changed:
    - P1.A collected with `PT_MUX_RAW_VIDEO_PROBESIZE=2000000`, `PT_MUX_RAW_VIDEO_ANALYZEDURATION=2000000`.
    - P1.B later became the final raw-video default: `PT_MUX_RAW_VIDEO_PROBESIZE=1000000`, `PT_MUX_RAW_VIDEO_ANALYZEDURATION=1000000`.
    - `PT_MUX_FFMPEG_LOGLEVEL=info` was only used for diagnostics; final closeout restores `warning`.
- Guardrails:
  - Do not test raw video probesize below `65536`.
  - Do not reintroduce `hevc_metadata=aud=insert`.
  - Do not change final mux intermediate TS probe before P1 results are collected.

## 2026-05-23 A8.P2.A Step 1 wired: independent intermediate TS probe config

- Implemented the Step 1 code path from `summary/summary_20260523_FIRST_CHUNK_LATENCY_A8_P2A_EXECUTION_CN.md`.
- Code changes:
  - `config.py` now exposes `MUX_INTERMEDIATE_TS_PROBESIZE` and `MUX_INTERMEDIATE_TS_ANALYZEDURATION`.
  - `_mux_intermediate_ts_probe_args()` in `pipeline/pynv_stream.py` now reads only those new settings.
  - Step 1 first landed with empty defaults; after the 16:58 baseline recheck, defaults were temporarily switched to P2.A.1 for UI-driven testing.
- Tests:
  - Added coverage for current defaults and explicit-config cases in `tests/test_pynv_mux_latency.py`.
  - Added command-level coverage for the pipe_ts final mux position of the new intermediate TS probe args.
  - Added default assertions in `tests/test_config_defaults.py`.
  - `python -m compileall config.py pipeline\\pynv_stream.py tests\\test_pynv_mux_latency.py tests\\test_config_defaults.py` passed.
  - `uv run python -m pytest tests\\test_pynv_mux_latency.py tests\\test_config_defaults.py tests\\test_pynv_startup_preflight.py` -> `18 passed`.
- Latest log:
  - 2026-05-23 16:58 UI/nPlayer run was P2.A.0 baseline recheck, not P2.A.1.
  - `pipe_ts final mux cmd` had no intermediate TS probe before the first `-f mpegts -i -`.
  - `first_chunk_breakdown`: `T1=164.0ms`, `T2a=478.4ms`, `T2b=2760.7ms`, `T3a=2763.7ms`, `T3b=2767.8ms`, `T3c=2768.7ms`, `T4=2778.0ms`, `total=2778.0ms`.
  - No codec-parameter regression in that run; final mux logged `Video: hevc ... 8192x4096`.
- State before moving to P2.A.2:
  - `MUX_RAW_VIDEO_PROBESIZE=1000000`
  - `MUX_RAW_VIDEO_ANALYZEDURATION=1000000`
  - `MUX_INTERMEDIATE_TS_PROBESIZE=16384`
  - `MUX_INTERMEDIATE_TS_ANALYZEDURATION=0`
  - `MUX_FFMPEG_LOGLEVEL=info`
- Next step:
  - Restart server, run UI playback again, and confirm `pipe_ts final mux cmd` contains `-probesize 16384 -analyzeduration 0` before the first `-f mpegts -i -`.
  - Then compare `T2b_final` and `total` against P2.A.0.

## 2026-05-23 A8.P2.A.1 data collected; defaults moved to P2.A.2

- Latest UI/nPlayer run at 17:17 confirmed P2.A.1 was active:
  - `pipe_ts final mux cmd` contained `-probesize 16384 -analyzeduration 0` before the first `-f mpegts -i -`.
  - Final mux recognized `Video: hevc ... 8192x4096`; no codec-parameter/audio-only regression was found in logs.
- P2.A.1 data:
  - `T1=171.4ms`
  - `T2a=473.9ms`
  - `T2b=1275.1ms`
  - `T3a=1291.1ms`
  - `T3b=1294.9ms`
  - `T3c=1300.4ms`
  - `T4=1978.4ms`
  - `total=1978.4ms`
- Interpretation:
  - Versus P2.A.0, `T2b` improved by `1485.6ms`, so P2.A.1 passes the >200ms step rule.
  - `T3c_output -> T4_reader` is now about `678ms`, so the remaining delay is no longer only final mux probe.
- Current temporary UI-test defaults:
  - `MUX_RAW_VIDEO_PROBESIZE=1000000`
  - `MUX_RAW_VIDEO_ANALYZEDURATION=1000000`
  - `MUX_INTERMEDIATE_TS_PROBESIZE=8192`
  - `MUX_INTERMEDIATE_TS_ANALYZEDURATION=0`
  - `MUX_FFMPEG_LOGLEVEL=info`
- Tests:
  - `python -m compileall config.py tests\\test_pynv_mux_latency.py tests\\test_config_defaults.py` passed.
  - `uv run python -m pytest tests\\test_pynv_mux_latency.py tests\\test_config_defaults.py tests\\test_pynv_startup_preflight.py` -> `18 passed`.
- Next step:
  - Restart server and run UI playback again for P2.A.2.
  - Confirm `pipe_ts final mux cmd` now contains `-probesize 8192 -analyzeduration 0` before the first `-f mpegts -i -`.
  - Compare `T2b_final` and `total` against P2.A.1.

## 2026-05-23 A8.P2.A gate update: Quest3 validation before P2.A.2

- Expert feedback after P2.A.1:
  - `T2b` improved sharply, but `T4_reader - T3c_output` expanded from about `9.3ms` to about `678.0ms`.
  - P2.A.2 must watch both `T2b` and `T4-T3c`, not only `T2b`.
  - Quest3 / DeoVR must validate P2.A.1 before continuing to 8192.
- Current temporary UI-test defaults were switched back to P2.A.1:
  - `MUX_INTERMEDIATE_TS_PROBESIZE=16384`
  - `MUX_INTERMEDIATE_TS_ANALYZEDURATION=0`
  - raw video remains `1000000/1000000`
  - mux loglevel remains `info`
- Updated `summary/summary_20260523_FIRST_CHUNK_LATENCY_A8_P2A_EXECUTION_CN.md` with:
  - P2.A.1 second-order `T4-T3c` note.
  - Quest3 gate before P2.A.2.
  - P2.A.2 stop/continue rules.
  - P2.A.3 default-not-do policy.
- Tests:
  - `python -m compileall config.py tests\\test_pynv_mux_latency.py tests\\test_config_defaults.py` passed.
  - `uv run python -m pytest tests\\test_pynv_mux_latency.py tests\\test_config_defaults.py tests\\test_pynv_startup_preflight.py` -> `18 passed`.
- Next step:
  - Restart server and play once from Quest3 / DeoVR.
  - Confirm logs still show `-probesize 16384 -analyzeduration 0` before the first `-f mpegts -i -`.
  - If Quest3 is video-ok, switch defaults to P2.A.2 `8192/0`; if Quest3 regresses, keep or roll back per P2.A doc.

## 2026-05-23 A8.P2.A.1 Quest3/SkyBox gate passed; defaults moved to P2.A.2

- Latest log at 17:37-17:38 contains two Quest3-side runs:
  - DeoVR: `AVProMobileVideo/15.6.3755 (Linux;Android 14) ExoPlayerLib/1.4.1`, profile `avpro`.
  - SkyBoxVR: `SKYBOX/2.0.2`, profile `libmpv`.
- Both runs used P2.A.1:
  - `pipe_ts final mux cmd` had `-probesize 16384 -analyzeduration 0` before the first `-f mpegts -i -`.
  - No `Could not find codec parameters`, audio-only, PPS, Broken pipe, or Traceback entries were found.
- P2.A.1 Quest3/SkyBox data:
  - DeoVR: `T1=173.8ms`, `T2a=496.4ms`, `T2b=1287.2ms`, `T3c=1306.8ms`, `T4=1939.1ms`, `total=1939.1ms`, `T4-T3c=632.3ms`.
  - SkyBoxVR: `T1=170.4ms`, `T2a=551.9ms`, `T2b=1350.2ms`, `T3c=1368.8ms`, `T4=2028.9ms`, `total=2028.9ms`, `T4-T3c=660.1ms`.
- Current temporary UI-test defaults:
  - `MUX_RAW_VIDEO_PROBESIZE=1000000`
  - `MUX_RAW_VIDEO_ANALYZEDURATION=1000000`
  - `MUX_INTERMEDIATE_TS_PROBESIZE=8192`
  - `MUX_INTERMEDIATE_TS_ANALYZEDURATION=0`
  - `MUX_FFMPEG_LOGLEVEL=info`
- Tests:
  - `python -m compileall config.py tests\\test_pynv_mux_latency.py tests\\test_config_defaults.py` passed.
  - `uv run python -m pytest tests\\test_pynv_mux_latency.py tests\\test_config_defaults.py tests\\test_pynv_startup_preflight.py` -> `18 passed`.
- Next step:
  - Restart server and run UI playback for P2.A.2.
  - Confirm logs show `-probesize 8192 -analyzeduration 0` before the first `-f mpegts -i -`.
  - Evaluate `T2b_final`, `total`, and `T4_reader - T3c_output` together.

## 2026-05-23 A8.P2.A.2 nPlayer tested; stopped at P2.A.1

- Latest nPlayer run at 17:48 confirmed P2.A.2 was active:
  - `pipe_ts final mux cmd` had `-probesize 8192 -analyzeduration 0` before the first `-f mpegts -i -`.
  - Final mux recognized `Video: hevc ... 8192x4096`.
  - No `Could not find codec parameters`, audio-only, PPS, Broken pipe, Traceback, or reader-waiting regression was found.
- P2.A.2 nPlayer data:
  - `T1=132.6ms`
  - `T2a=426.8ms`
  - `T2b=1300.5ms`
  - `T3a=1318.4ms`
  - `T3b=1322.8ms`
  - `T3c=1327.5ms`
  - `T4=2056.1ms`
  - `total=2056.1ms`
  - `T4-T3c=728.6ms`
- Decision:
  - Versus P2.A.1 nPlayer (`T2b=1275.1ms`, `total=1978.4ms`), 8192 did not improve `T2b` and worsened total.
  - Stop the P2.A ladder at `16384/0`; do not run P2.A.3 `4096/0`.
- Current default candidate:
  - `MUX_INTERMEDIATE_TS_PROBESIZE=16384`
  - `MUX_INTERMEDIATE_TS_ANALYZEDURATION=0`
- Tests:
  - `python -m compileall config.py tests\\test_pynv_mux_latency.py tests\\test_config_defaults.py` passed.
  - `uv run python -m pytest tests\\test_pynv_mux_latency.py tests\\test_config_defaults.py tests\\test_pynv_startup_preflight.py` -> `18 passed`.

## 2026-05-23 A8.2 max_interleave_delta single-point test enabled

- Expert follow-up changed the remaining priority:
  - P2.A is stopped at `16384/0`.
  - P2.B / P2.C are deferred because their mechanism targets PAT/PMT/probe, while the remaining bottleneck is `T4_reader - T3c_output`.
  - A8.2 is now the direct target for the `630-730ms` post-output reader gap.
- Code/test state:
  - A8.2 temporarily used `PASSTHROUGH_AUDIO_MPEGTS_INTERLEAVE_DELTA=0`; final closeout restores `500000000`.
  - P2.A defaults remain `MUX_INTERMEDIATE_TS_PROBESIZE=16384`, `MUX_INTERMEDIATE_TS_ANALYZEDURATION=0`.
  - `tests/test_pynv_mux_latency.py` asserts `-max_interleave_delta 0` is present in the pipe_ts final mux command.
  - `run_server.bat` does not override `PT_PASSTHROUGH_AUDIO_MPEGTS_INTERLEAVE_DELTA`, so the config default will apply to UI runs.
- Updated docs:
  - `summary/summary_20260523_FIRST_CHUNK_LATENCY_A8_2_PATCH_CN.md` now marks A8.2 as the next priority after P2.A.
  - `summary/summary_20260523_FIRST_CHUNK_LATENCY_A8_P2A_EXECUTION_CN.md` records the A8.2 follow-up and says not to combine with P2.B/P2.C.
- Tests:
  - `python -m compileall config.py tests\\test_pynv_mux_latency.py tests\\test_config_defaults.py` passed.
  - `uv run python -m pytest tests\\test_pynv_mux_latency.py tests\\test_config_defaults.py tests\\test_pynv_startup_preflight.py` -> `18 passed`.
- Next run:
  - Restart server and play once, preferably nPlayer first.
  - Confirm command contains both `-probesize 16384 -analyzeduration 0` and `-max_interleave_delta 0`.
  - Main success metric: `T4_reader - T3c_output < 100ms` and total near/below `1500ms`.
  - Regression checks: no stall, audio sync issue, audio-only mode, black screen, `Could not find codec parameters`, or reader waiting warnings.

## 2026-05-23 A8.2 max_interleave_delta=0 tested; reverted

- Latest nPlayer run at 19:09 confirmed A8.2 was active:
  - `pipe_ts final mux cmd` had `-probesize 16384 -analyzeduration 0`.
  - The same command had `-max_interleave_delta 0`.
  - Final mux recognized `Video: hevc ... 8192x4096`.
  - No `Could not find codec parameters`, audio-only, PPS, Broken pipe, Traceback, or reader-waiting regression was found.
- A8.2 data:
  - `T1=143.6ms`
  - `T2a=437.3ms`
  - `T2b=1268.2ms`
  - `T3a=1285.6ms`
  - `T3b=1290.0ms`
  - `T3c=1296.3ms`
  - `T4=1959.8ms`
  - `total=1959.8ms`
  - `T4-T3c=663.5ms`
- Decision:
  - A8.2 did not reduce the `T4-T3c` gap; it remained inside the prior `630-730ms` band.
  - Total improved only `18.6ms` versus P2.A.1 nPlayer, which is noise-level.
  - Reverted `PASSTHROUGH_AUDIO_MPEGTS_INTERLEAVE_DELTA` default to `500000000`; do not lock `0`.
- Current stable default candidates:
  - `MUX_RAW_VIDEO_PROBESIZE=1000000`
  - `MUX_RAW_VIDEO_ANALYZEDURATION=1000000`
  - `MUX_INTERMEDIATE_TS_PROBESIZE=16384`
  - `MUX_INTERMEDIATE_TS_ANALYZEDURATION=0`
  - `PASSTHROUGH_AUDIO_MPEGTS_INTERLEAVE_DELTA=500000000`
- Tests:
  - `python -m compileall config.py tests\\test_pynv_mux_latency.py tests\\test_config_defaults.py` passed.
  - `uv run python -m pytest tests\\test_pynv_mux_latency.py tests\\test_config_defaults.py tests\\test_pynv_startup_preflight.py` -> `18 passed`.

## 2026-05-23 Track A final closeout config

- Track A final archive is `summary/summary_20260523_TRACK_A_FINAL_ARCHIVE_CN.md`.
- Kept the validated low-latency defaults:
  - `MUX_RAW_VIDEO_PROBESIZE=1000000`
  - `MUX_RAW_VIDEO_ANALYZEDURATION=1000000`
  - `MUX_INTERMEDIATE_TS_PROBESIZE=16384`
  - `MUX_INTERMEDIATE_TS_ANALYZEDURATION=0`
  - `PASSTHROUGH_AUDIO_MPEGTS_INTERLEAVE_DELTA=500000000`
- Restored temporary diagnostic defaults:
  - `MUX_LATENCY_DIAG_VERBOSE=0`
  - `MUX_FFMPEG_LOGLEVEL=warning`
- Updated roadmap docs:
  - `summary/summary_20260522_ROADMAP_POST_TRT_WARMUP_CN.md` marks Track A completed.
  - `summary/summary_20260523_ROADMAP_TRACK_A_BREAKDOWN_CN.md` points to the final archive and records the A7/A8 final settings.

## 2026-05-23 AV sync slate burst mitigation

- Root cause reference:
  - `summary/summary_20260523_AV_SYNC_SLATE_BURST_ROOT_CAUSE_CN.md`
- Implemented mitigation:
  - Changed `PASSTHROUGH_AUDIO_MPEGTS_SLATE_BURST_FRAMES` default from `90` to `1`.
  - Moved slate video pacing to before frame encode/write after the configured burst, so only the configured initial frames are truly immediate.
  - Added `burst_frames` and `fps` to the `slate video begin` log line for objective sync validation.
- Expected effect:
  - Slate startup still sends one FORCEIDR/SPS/PPS frame immediately for strict player video detection.
  - Subsequent slate frames track wallclock silent AAC, so video PTS lead should stay near one frame instead of seconds.
- Automatic validation:
  - Added `tools/check_av_sync_slate.py`.
  - After restarting and playing a passthrough chapter once, run:
    - `python tools\check_av_sync_slate.py --max-lead 0.10`
    - `python tools\check_av_sync_slate.py --json --max-lead 0.10`
  - Success means estimated slate video lead is <= 100 ms. The estimate is `frames / fps - elapsed` from server log records.
- Tests:
  - `python -m compileall config.py pipeline\pynv_stream.py tools\check_av_sync_slate.py tests\test_pynv_mux_latency.py tests\test_config_defaults.py` passed.
  - `uv run python -m pytest tests\test_pynv_mux_latency.py tests\test_config_defaults.py` -> `18 passed`.
- Runtime validation still needed:
  - Restart the server and play TensorRT and CUDA chapters from the middle.
  - Check no `Could not find codec parameters`, no audio-only startup, and no startup stall.
  - Run `tools/check_av_sync_slate.py`; if any player regresses on video detection, test `PT_PASSTHROUGH_AUDIO_MPEGTS_SLATE_BURST_FRAMES=4` or fall back to the planned audio offset correction.

## 2026-05-23 AV sync TensorRT slate validation

- User first ran the checker against an AAC cache-hit playback:
  - Output: `no slate video records found: latest playback used cached AAC and bypassed slate`.
  - Interpretation: this path does not exercise slate and cannot validate slate burst behavior.
- User then forced a slate path and ran:
  - `uv run python tools\check_av_sync_slate.py --max-lead 0.10`
  - Result: `worst slate video lead: 0.000s (sid=1 frames=86 fps=59.940 elapsed=1.450s burst=1)`.
  - JSON details: `video_pts_sec=1.4347681014347682`, `elapsed_sec=1.45`, `estimated_video_lead_sec=0.0`.
- Conclusion:
  - TensorRT slate burst desync is fixed for this sample.
  - Old `burst=90` would have allowed a large positive video lead; current `burst=1` plus pre-send pacing keeps slate video at or slightly behind wallclock audio.
- Remaining checks:
  - Confirm subjective playback no longer has the prior multi-second audio lag.
  - Repeat once on CUDA and once on a strict player if needed, mainly to check no video-detection regression.

## 2026-05-23 AV sync CUDA slate validation

- User ran the same checker on CUDA mode with a forced slate path.
- Result:
  - `worst slate video lead: 0.000s (sid=1 frames=90 fps=59.940 elapsed=1.507s burst=1)`
  - JSON detail: `video_pts_sec=1.5015015015015016`, `elapsed_sec=1.507`, `estimated_video_lead_sec=0.0`.
- Conclusion:
  - CUDA mode also stays synchronized under the new slate pacing.
  - The fix is not TensorRT-specific; it covers both CUDA and TRT paths.

## 2026-05-23 AV sync live TS delta follow-up

- User still hears strong A/V desync on real-device TensorRT playback of
  `videos/urvrsp00566_1_8k.mp4`, and green/alpha at the same timestamp sound
  almost identically offset.
- `tools/check_mpegts_sync.py` was validated on the two local captures:
  - `debug_output/urvrsp_videos_180_green_capture.ts`:
    `video_start=1.400s`, `audio_start=2.424s`, `audio-minus-video=+1.024s`.
  - `debug_output/urvrsp_videos_180_alpha_capture.ts`:
    `video_start=2.423s`, `audio_start=1.400s`, `audio-minus-video=-1.023s`.
- Interpretation:
  - The sign flips because the earlier PTS stream changes between runs, but
    the absolute live TS offset remains about `1.024s`.
  - This can still sound like the same subjective desync because players anchor
    playback to the earliest usable PTS/PCR and the other stream arrives about
    one second away from that anchor.
- Current conclusion:
  - `check_av_sync_slate.py` only proves slate pacing is fixed.
  - The remaining issue is in final live TS mux/audio handoff timestamp
    alignment, so fixes should be validated with `tools/check_mpegts_sync.py`
    on captured TS output.

## 2026-05-23 AV sync final mux fflags fix

- User correctly challenged the proposed `1.024s` offset as an empirical
  hard-code. That approach was abandoned.
- Offline two-stage pipe_ts reproduction found the real trigger:
  - Final mux with `-fflags +genpts+nobuffer+flush_packets` produced about
    `1.02s` A/V start delta.
  - Final mux with `-fflags +genpts` reduced the same cache-file path to
    `0.021333s`.
  - The slate/TCP simulation also reduced both input-order variants to
    `0.021333s`.
- Implemented:
  - Added `_pipe_ts_final_mux_fflags()` in `pipeline/pynv_stream.py`.
  - `_open_pipe_ts_muxer()` and `_open_slate_pipe_ts_muxer()` now keep
    `nobuffer/flush_packets` on the raw-video intermediate mux, but use only
    `+genpts` on the final mux.
  - No `-itsoffset` compensation and no hard-coded sync seconds are used.
- Validation:
  - `python -m compileall config.py pipeline\pynv_stream.py tests\test_pynv_mux_latency.py tests\test_config_defaults.py` passed.
  - `uv run python -m pytest tests\test_pynv_mux_latency.py tests\test_config_defaults.py` -> `19 passed`.
  - `tools/check_mpegts_sync.py` on offline cache/TCP fixed variants:
    all reported `abs_delta_sec=0.021333`.
- Required live validation after server restart:
  - Capture `videos/urvrsp00566_1_8k.mp4?t=180` once in green and once in alpha.
  - Run `uv run python tools\check_mpegts_sync.py <capture.ts> --max-delta 0.10`.
  - Expected result: abs delta <= 0.10s for both CUDA and TensorRT.

## 2026-05-23 AAC cache disabled for diagnostic retest

- User reported that even after final mux `+genpts`, CUDA and TensorRT still
  sound like audio is late by 1-2s.
- Logs showed the tested green paths were all `audio cache hit` and ffmpeg
  warned `Estimating duration from bitrate` for raw ADTS `.aac` cache inputs.
- Temporary diagnostic change:
  - `PASSTHROUGH_AUDIO_MPEGTS_CACHE` default changed from `1` to `0`.
  - This bypasses `runtime_cache/audio/*.aac` on the next restarted server run.
- Purpose:
  - Determine whether raw ADTS AAC cache seeking is the remaining cause.
  - This is a diagnostic switch, not yet the final audio-cache design.

## 2026-05-23 cache-off path kept live without slate audio skew

- User pointed out the switch must remain meaningful for first playback.
- Updated `pipe_ts` audio selection so `PASSTHROUGH_AUDIO_MPEGTS_CACHE=0`
  does not fall back to single-stage `setts`.
- Current behavior:
  - cache hit: use cached AAC input as before.
  - cache miss with cache enabled: build cache and use slate/TCP.
  - cache disabled: skip disk cache and have the pipe_ts final mux read source
    MP4 audio directly. This avoids the slate/TCP path's silence-then-real-AAC
    concatenation, which can make the real audio content late by the slate
    startup duration.
- Validation:
  - `uv run python -m pytest tests\test_pynv_mux_latency.py tests\test_config_defaults.py`
    -> `20 passed`.
  - Added `tools/capture_live_mpegts.py` so a live URL can be captured and fed
    into `tools/check_mpegts_sync.py` for objective A/V start-delta validation.

## 2026-05-23 live sync objective retest and source-FPS UI migration

- User reported that subjective playback still sounded unchanged after cache
  was disabled and source MP4 audio was used directly in the pipe_ts final mux.
- Captured current live output for
  `Downloads/VR/VR108/urvrsp-566/4k2.me@urvrsp00566_1_8k.mp4`:
  - `debug_output/urvrsp_current_alpha_t0.ts`
  - `debug_output/urvrsp_current_alpha_t180.ts`
  - `debug_output/urvrsp_current_green_t180.ts`
- Objective mux checks:
  - alpha `t=180`: `audio_minus_video=-0.021333s`
  - green `t=180`: `audio_minus_video=-0.021333s`
  - Both are inside the 100 ms tolerance; there is no 1-2 s TS start-time
    offset in the current output.
- Added `tools/check_live_audio_alignment.py`:
  - Extracts mono PCM from a captured TS and from a source time window.
  - Uses normalized audio correlation to locate the captured audio content in
    the source timeline.
  - Validation on current captures:
    - alpha `t=180`: matched source `179.9786875s`, offset `-0.0213125s`,
      correlation `0.994846`.
    - green `t=180`: same result.
  - This proves the live output audio content is aligned with the requested
    source time to about one AAC frame, not delayed by seconds.
- PyNv decoder sanity check on `videos/urvrsp00566_1_8k.mp4`:
  - For the `t=180`, `fps=30` run, logs used `src_idx=10789`.
  - `PyNvSimpleDecoder.frame_at(10789).pts / 60000 = 179.963117s`.
  - This is within a few source frames of requested time; PyNv indexing is not
    producing a 1-2 s future frame.
- Remaining plausible true-device cause:
  - UI persisted the old diagnostic default `PT_PASSTHROUGH_MAX_FPS=30`, while
    the source is 59.94 fps.
  - The generated TS is timestamp-correct at 30 fps, but a strict/VR live player
    may still behave better when the live stream preserves the source cadence.
- Implemented:
  - `ui/settings.py` default `passthrough_max_fps` changed from `30` to `0`
    (source FPS).
  - New settings migration `20260523_source_fps_live_sync` resets saved legacy
    value `30` back to `0`, while preserving explicit non-default values such
    as `24`.
  - `Settings.server_env()` now always sets
    `PT_PASSTHROUGH_PRODUCER_REALTIME_PACING=1`, so source-FPS live output is
    still paced instead of burst-sent.
- Validation:
  - `uv run python -m compileall ui\settings.py tests\test_settings.py tools\check_live_audio_alignment.py config.py pipeline\pynv_stream.py tools\capture_live_mpegts.py tools\check_mpegts_sync.py`
    passed.
  - `uv run python -m pytest tests\test_settings.py tests\test_pynv_mux_latency.py tests\test_config_defaults.py`
    -> `30 passed`.
- Next real-device validation:
  - Restart through the UI so the new settings migration takes effect.
  - Confirm the server log changes from `PASSTHROUGH_MAX_FPS=30.00` /
    `output_fps=30.000` to source cadence around `59.940`.
  - Retest the same TensorRT/CUDA scene. If hearing is still off, captured TS
    and audio-correlation evidence indicate the remaining issue is likely
    client playback behavior or source-content perception rather than server
    mux/content timing.

## 2026-05-23 slate disabled for real-device A/B

- User retested source-FPS live output and still heard severe A/V desync.
- User clarified this means the generated **video slate** must be disabled,
  not merely the audio-side slate/TCP path.
- Per user request, added an explicit config-level video slate switch:
  - `PASSTHROUGH_MPEGTS_VIDEO_SLATE` default is now `0`.
  - `PT_PASSTHROUGH_AUDIO_MPEGTS_SLATE` remains as a legacy alias.
  - `PASSTHROUGH_AUDIO_MPEGTS_SLATE` now mirrors
    `PASSTHROUGH_MPEGTS_VIDEO_SLATE`.
- Code paths guarded:
  - `_open_muxer()` only calls `_start_slate_audio_server()` when
    `PASSTHROUGH_MPEGTS_VIDEO_SLATE` is true.
  - The worker only starts `slate_video_loop()` when
    `_slate_audio_addr is not None` and `PASSTHROUGH_MPEGTS_VIDEO_SLATE` is
    true.
- Important interpretation:
  - With current `PASSTHROUGH_AUDIO_MPEGTS_CACHE=0`, recent logs already show
    `audio cache disabled; using source audio in pipe_ts final mux`, which
    bypasses the slate/TCP path entirely.
  - This change mainly guarantees that if cache is re-enabled and a cache miss
    occurs, the old slate path will not start by default.
- Validation:
  - `uv run python -m compileall config.py tests\test_config_defaults.py tests\test_pynv_mux_latency.py tests\test_settings.py`
    passed.
  - `uv run python -m compileall config.py pipeline\pynv_stream.py tests\test_config_defaults.py tests\test_pynv_mux_latency.py`
    passed after adding the explicit video-slate switch.
  - `uv run python -m pytest tests\test_config_defaults.py tests\test_pynv_mux_latency.py`
    -> `21 passed`.
  - Added `test_pipe_ts_cache_miss_with_video_slate_disabled_does_not_start_slate`
    to prove cache-miss with video slate disabled does not call
    `_start_slate_audio_server()`.
- Runtime check after restart:
  - `server.log` should not contain `slate audio server listening` or
    `slate video begin` for the tested playback.
  - Current cache-off path should continue logging
    `audio cache disabled; using source audio in pipe_ts final mux`.

## 2026-05-23 external AV-sync review brief

- User retested with video slate disabled:
  - Subjective playback improved, but audio still sounds about 1 second late.
  - User then tried another player; issue remained.
- Prepared external-review summaries for a reviewer who cannot see the source:
  - `summary/summary_20260523_AV_SYNC_EXTERNAL_REVIEW_CN.md`
  - `summary/summary_20260523_AV_SYNC_EXTERNAL_REVIEW_EN.md`
- The brief emphasizes:
  - early slate/cache path was a real risk but is now bypassed by default;
  - current cache-off/no-slate path still sounds wrong on device;
  - previous server-side captures showed TS start-time and audio-content
    alignment within about 21 ms;
  - unresolved contradiction is likely client/live-TS/Range/PCR behavior or
    missing objective video-content correlation.
- User asked to add two missing points to the external-review brief:
  - project background that video and audio are processed separately and only
    rejoined at final mux;
  - keyframe/GOP seek risk for mid-stream playback, while noting that similar
    desync from `t=0` makes keyframe rollback insufficient as a complete
    explanation.
- Updated both CN/EN external-review summaries with those points.
- Latest local capture attempt using the bare key
  `/passthrough_live/urvrsp00566_1_8k.mp4` returned HTTP 403, likely because
  the running server's media-library key mapping did not match that bare name.
  This was not treated as AV-sync evidence.
