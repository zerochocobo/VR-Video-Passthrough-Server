# 2026-07-01 Handover

## SI ducking strength preset

- Added SI ducking strength presets in `utils/si_filter.py`:
  - `light`: threshold `0.03`, ratio `2.5`, release `400`
  - `normal`: threshold `0.025`, ratio `5`, release `600`
  - `strong`: threshold `0.015`, ratio `10`, release `800`
- Kept attack fixed at `30` ms and makeup fixed at `1`.
- Extended `SIMixParams`, runtime state, `/control/si_mix`, UI settings persistence, and server env with `duck_preset` / `si_duck_preset` / `PT_SI_DUCK_PRESET`.
- Updated SI settings dialog to keep the existing "Lower original during SI" checkbox and enable a "Ducking strength" combo with Light / Normal / Strong when checked. Default is `normal`.
- Updated Chinese, English, and Japanese UI translations.
- Ensured progressive SI AAC cache digest includes the selected duck preset, so changing strength rebuilds the mixed sidecar.

## Verification

- Syntax checked edited Python files without writing pycache.
- JSON translations validated using UTF-8 BOM aware reads.
- Ran `.\.venv\Scripts\python.exe -m pytest -p no:cacheprovider tests\test_si_mix.py`: 16 passed.
- Ran `.\.venv\Scripts\python.exe -m pytest -p no:cacheprovider tests\test_si_virtual_mp4.py`: 15 passed.

## Notes

- Running pytest with the system Python failed because `fastapi` is not installed there; the project `.venv` has the required dependencies.
- `python -m compileall ...` could not write some `__pycache__` files due permission errors, so validation used in-memory `compile(...)` instead.
